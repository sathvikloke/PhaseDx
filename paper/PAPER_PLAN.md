# PAPER_PLAN.md

Written 2026-07-29, after reading `paper/audit_results.md`, `paper/audit_targets.md`,
`paper/protocol.md`, `paper/checklist.md`, `pipeline_out/report/RESULTS.md`,
`pipeline_out/robustness/s09_robustness.json`, `pipeline_out/recon_fidelity/`,
`pipeline_out/rempe/` and all 20 cards in `pipeline_out/trivial_baselines/`.

Everything below is written on the assumption that a hostile reviewer will read it, and
that the most dangerous person in the review pool is someone who already knows Yagis 2021,
Badgeley 2019 and Ong Ly 2024. Section 8 is the list of things they will say.

---

## 0. Bottom line, before the argument

> **REVISED 2026-07-29, after the four-screener prevalence screen and the revised benchmark
> audit came back. RE-ANCHORED the same evening: the flagship is now the RSNA ICH
> slice-to-patient collapse on all 18,938 patients, which needs no published comparator, and
> the audit's headline is the *distribution* of the trivial fraction rather than a count of
> verdicts. The master number-and-provenance document is `paper/FINDINGS.md`; this plan
> defers to it wherever they differ, and `FINDINGS.md` must itself be brought forward to
> §0.1 of `paper/audit_results.md`.**

**The flagship claim no longer depends on an unreviewed preprint.** It previously did: the
headline was a zero-image positional baseline reaching 0.851 [0.816, 0.887] slice AUROC
against Rempe et al.'s reported 0.809–0.861 and falling to 0.424 [0.298, 0.547] at the
patient level, on 46 test patients, against a comparator with no journal-ref two years after
posting. A hostile editor's disqualifying sentence wrote itself. **The flagship is now:**

> On the RSNA 2019 Intracranial Haemorrhage benchmark — **752,802 slices from 18,938
> patients**, the benchmark whose own official metric is per-slice — a 20-bin estimate of
> P(haemorrhage | relative slice position), fitted on the training slices of a
> subject-disjoint five-fold split and applied out of fold, reaches **slice AUROC 0.737
> [0.735, 0.740]** and **patient AUROC 0.453 [0.445, 0.461]** on the same score vector.
> Nothing changes between the two readings except the unit at which the ranking is performed.
> The patient-level interval lies wholly below chance; scoring each patient by their most
> suspicious slice instead gives exactly 0.500. All six official labels behave the same way,
> with gaps of 0.205–0.307.

Why this is the right anchor: **no published number enters it**, so there is no reproduction
dispute and no comparator's peer-review status to attack; the cohort is ~400× the prostate
arm; it holds on six labels rather than one; and it was recomputed by a second implementation
sharing no code with the first
(`pipeline/audit_prep/rsna_ich_unit_collapse.py` →
`pipeline_out/trivial_baselines/rsna_ich_unit_collapse.json`, plus the released tool's own
full-cohort card `rsna_ich_any_slice_full.json`).
Rempe et al. is demoted to a **worked example, labelled every
time as resting on a preprint**.

**And the audit's headline is now continuous, not categorical.** "Six MATCHED, nine PARTIAL,
three NOT MATCHED" discarded the information the trivial fraction exists to carry and staked
the paper on a threshold only the preprint's rows crossed. Replaced by:

> Against **peer-reviewed** comparators, on the same metric and the same evaluation unit,
> taking each benchmark-arm's strongest published system, the median trivial fraction is
> **0.469** (IQR 0.437–0.490, range −0.002 to 0.613, n = 9). Eight of those nine rows lie in
> 0.395–0.613; the ninth is LUNA16 at −0.002. The six RSNA ICH subtype rows — peer-reviewed,
> identical cohort, identical metric and unit — span **0.395–0.613**: roughly 40–60% of the
> published margin over chance, per subtype, with no pixels.

`paper/trivial_fraction_distribution.{json,md}`, generated by
`pipeline/audit_prep/trivial_fraction_distribution.py`.

**On today's evidence this is still not an IF 15+ paper, and it should not be sent to one —
but the disqualifying dependency is gone.**

**Benchmarks: the verdict count went backwards, and it no longer matters as much.** The
precondition was *three or more independent benchmarks MATCHED against peer-reviewed numbers
on the same metric and unit*. Reaching RSNA ICH added a fourth valid peer-reviewed
head-to-head, so the count of **defensible comparisons** rose from three to **four** (RSNA
ICH, LUNA16, DeepLesion, PI-CAI). **None of the four is MATCHED**, and all six MATCHED rows
still trace to one arXiv preprint, so **the count of benchmarks matched against a
peer-reviewed number is zero, not one.** `FINDINGS.md` §3.1. What changed is that the paper
no longer needs that count: the flagship is comparator-free and the audit's headline is a
distribution with a defensible median, both of which survive deleting Rempe et al. entirely.

**Prevalence landed, and it is the strongest single number in the paper. FINAL NUMBERS,
2026-07-29, after the adjudication remedy, the access-recovery pass, the full v1.2 re-code and
four reserve blocks — all earlier prevalence figures in this plan are superseded.** Across 250
PubMed-sampled volumetric-imaging classification papers drawn in permutation order under the
pre-registered extension rule, 345 coded records over 300 distinct papers including the
post-hoc fifth block: **not one paper reports a measured zero-image baseline of any kind**
(P1 = 0/91 complete case, 0.0% [0.0%, 4.1%]; **zero TRUE flags on any of the four zero-image
sub-flags anywhere in the raw coding**, including excluded and unreachable records). **Only
1 of 91 readable papers reports the positional distribution of its labels** (S5 = 1/91, 1.1%
[0.2%, 6.0%]). `FINDINGS.md` §3.1, §3.2, §3.7.

**Two of the three earlier caveats are now cleared; the binding one is not, and it must be
stated before the number, not after.**

- **(i) STILL BINDING — access.** Full text was unreachable for **32.6% [25.3%, 40.9%]** of the
  eligible set, more than 2× the 15% threshold at which the protocol makes the *bounding*
  interval the headline. So the reportable P1 is **[0.0%, 32.6%]**, not 0.0%. **The extension
  did not fix this and could not**: the unreachable rate was 35.6%, 34.6%, 22.7% and 32.1% in
  four separately screened blocks, and 150 extra papers moved the pooled figure by under four
  points. Only recovered full texts move it. A previously published repository number —
  29.6%, in `paper/prisma_flow.json` and the old `FINDINGS.md` §1.4a — was an artefact of
  applying the access-recovery overlay without codebook rule D1, and is corrected here in the
  direction that hurts us. `FINDINGS.md` §1.1.
- **(ii) CLEARED — sample size.** 91 included papers against a pre-registered target of 75. The
  extension rule fired and stopped at permutation position 260. The complete-case zero-count
  upper bound is now **4.1%**, inside the 4.9% the protocol asked for.
- **(iii) PARTLY CLEARED — agreement.** The remedy was run. Post-amendment the primary flag
  gives raw **95.6%**, Fleiss' κ **0.932 [0.777, 1.000]**, AC1 0.934, 14/15 unanimous, and
  **both pre-registered floors are met**. But that number is a *counterfactual re-encoding of
  the same four sealed files* under two amendments that add a missing level — not an
  independent re-rating — and all 200 reserve records are single-coded. The paper's entire
  reliability estimate still rests on 15 papers. Report the pre-reconciliation failure first
  (raw 65.6%, κ −0.015), the diagnosis second (the codebook had no "not assessable" level), the
  post-amendment numbers third, and the "same four files" qualifier in the same paragraph.
  `FINDINGS.md` §1.2, §4.

What survives all of that is real, useful and publishable, and is now *stronger* than it was:
a measured, reproducible, cross-benchmark demonstration that the *unit of evaluation* is
doing work the reported number is credited to; **a pre-registered field-level prevalence
showing the check is never run**; a remedy metric; a checklist; and a tool that lets anyone
audit a benchmark they will never hold. That is a strong methods/audit paper for a specialist
journal, with a genuine field-level result attached. It is not a Nature Medicine paper, and
pretending otherwise wastes six months.

**The rank-inversion arm came back, and it is a null. ADDED 2026-07-29 (late).** The question
that would have changed the paper's tier was whether the choice of evaluation unit changes
*which method you would conclude is best*. It was tested properly — 21 method configurations,
five cohorts, one shared subject-clustered bootstrap of 2,000 replicates, between-unit rank
disagreement compared against the within-unit disagreement that resampling subjects produces
with the unit held fixed, inversion pairs requiring both orderings to survive Holm over every
pair examined. **Of 447 method pairs, 204 point in opposite directions at the two units and
zero survive. In no cohort does between-unit disagreement exceed within-unit resampling noise
at both units.** On three of five cohorts, two disjoint halves of the same subjects rank the
same methods in *opposite* orders (split-half τ = −0.42, −0.24, −1.00 at the patient unit).
The licensed claim is therefore **"a benchmark of this size cannot rank methods at either
unit"**, not "the unit changes the ranking". One narrower effect *is* supported: on the brain
confound cohort the paired unit × condition interaction is **+0.028 AUROC [0.015, 0.041],
p = 0.001**, same direction in all six architectures, Holm-supported in four, sign-flipping in
two — but neither ordering is individually distinguishable from a tie, so no method selection
follows. In the published literature inversions are real and change winners (CAMELYON16's own
two boards, τ = 0.754; a 13-method reproduction where the winner changes, τ_a = 0.603), but
only two published cases report intervals at both units and in both the inversion lies inside
them. **Full analysis and every number: `paper/rank_inversion.md`. Results file:
`pipeline_out/rankinversion.json`. Estimator: `pipeline/s16_rankinversion.py`, 65/65
self-tests.**

**Tier consequence, straight: the null leaves the paper exactly where it was.** A
well-supported inversion — especially one visible in published work — would have been the
strongest result in the project and would have made npj Digital Medicine a genuine target. It
did not land. Radiology: AI stays primary; npj DM stays a shot; the null goes in as a reported
result in §3.3bis of the draft and as two Limitations paragraphs. See §3.4 for the one route
that could still change this.

Recommended venue, **unchanged in rank but for a changed reason**: **Radiology: Artificial
Intelligence** (primary), with a pre-submission enquiry to **npj Digital Medicine** sent
first. The enquiry is now materially more likely to land than it was this morning, because
the prevalence screen is exactly the kind of field-level evidence npj DM publishes and it is
the piece Ong Ly et al. 2024 does not have. See §3.2 for the revised probability call and
§3.3 for the honest IF 15+ verdict.

---

## 1. The one honest sentence

> On the RSNA 2019 Intracranial Haemorrhage benchmark — **752,802 slices from 18,938
> patients**, the benchmark whose own official metric is per-slice — a model that never sees
> a pixel, fitted only on where a slice sits in its stack, reaches **slice AUROC 0.737
> [0.735, 0.740]** and, reading the *same score vector* one level up, **patient AUROC 0.453
> [0.445, 0.461]**. Nothing changes between the two readings except the unit; only the first
> is what a paper would print; and all six of the benchmark's official labels behave the
> same way.

**This sentence contains no published number**, which is exactly why it is now first. It is
computed entirely from our own runs on a public label file, so it depends on no comparator
and cannot be attacked on comparability grounds — which is the attack available against every
MATCHED row. It has been produced by four routes (two implementations sharing no code, two
cohort sizes, plus a replication at the published paper's own split geometry) agreeing to
0.003 AUROC. `paper/audit_results.md` §0.1, §3.7;
`pipeline_out/trivial_baselines/rsna_ich_unit_collapse.json`,
`rsna_ich_any_slice_full.json`.

Second sentence — the audit as a continuous measure rather than a verdict count:

> Against **peer-reviewed** published numbers, on the same metric and the same evaluation
> unit, a pixel-blind model reaches a median of **0.469** of the reported margin over chance
> (IQR 0.437–0.490, range −0.002 to 0.613 over nine benchmark-arms). On the one benchmark
> supplying six peer-reviewed head-to-heads on an identical cohort, the fractions run
> **0.395–0.613** across its subtypes: roughly 40–60% of the published margin, per subtype,
> with no pixels.

Third sentence, and it must never be dropped:

> The effect is not universal and the measure is not always positive: on DeepLesion, whose
> labels are anatomical regions, the positional model is high at *both* units (0.977 slice /
> 0.954 patient for pelvis); on LUNA16 it is at chance at both (0.534 / 0.581) and scores
> 0.0020 on the challenge's own CPM metric — *below* its random-score reference of 0.0027 —
> against a published >0.95 sensitivity at <1 false positive per scan, for a trivial fraction
> of **−0.002**; and on PI-CAI the positional baseline is exactly 0.500, the correct
> registration of "inapplicable". A measure that always fires measures nothing.

**Old first sentence, retired.** *"Across six public medical imaging benchmarks … matching
the published slice-level headline of one benchmark (0.854 [0.812, 0.891] against a reported
0.861)."* It staked the paper's flagship on Rempe et al., an arXiv preprint with no
journal-ref two years after posting, on a 46-patient test arm. That is now §3.2 of the draft,
a **worked example labelled as resting on a preprint**, and nothing depends on it.

**Third sentence, added 2026-07-29 — and on the current evidence this is the one that
carries the paper, because it depends on no benchmark, no comparator and no preprint:**

> In a pre-registered screen of 250 randomly sampled published volumetric-imaging
> classification papers — coded independently by four screeners on the 15-paper overlap set,
> extended into the reserve in permutation order until the pre-registered target of 75 included
> papers was met — **none of the 91 papers whose full text could be obtained reported a measured
> zero-image baseline of any kind** (0/91, 95% Wilson [0.0%, 4.1%]; **bounding interval over the
> 32.6% of the eligible set that was unreachable, [0.0%, 32.6%]**, which is the headline the
> protocol requires), and **only one reported the positional distribution of its labels**
> (1/91, 1.1% [0.2%, 6.0%]). Across all 345 coded records covering 300 distinct sampled papers,
> including every excluded and unreachable one, not a single record carries a positive code on
> any of the four zero-image sub-flags. The only non-imaging comparator found in the readable
> set was the clinical-variables arm of a nomogram (5/91, 5.5% [2.4%, 12.2%]) — a comparison
> that asks whether imaging adds to clinical data, not whether the benchmark is solvable
> without pixels.

Source for every figure: `paper/FINDINGS.md` §3, generated by
`paper/screen/analysis/pool_final.py`. The bounding interval must travel with the point
estimate in every appearance; the protocol makes it the headline, not a sensitivity. The
censoring-free sentence — 345 records, zero positive codes — is the one form of the result that
survives every objection about the denominator, and it is the sentence to lead a cover letter
with.

---

## 2. Claims the evidence does NOT support — write these on the wall

Each of these is a sentence a draft will drift towards. None of them may appear.

| Forbidden claim | Why it fails | What may be said instead |
|---|---|---|
| "Trivial baselines match published performance on medical imaging benchmarks." | Six MATCHED rows, all from one benchmark, whose comparator is a preprint. Zero MATCHED against peer-reviewed comparators. (`audit_results.md` §0.2, §2.0) | "Against peer-reviewed published numbers, on the same metric and unit, a pixel-blind model reaches a median 0.469 of the reported margin over chance (IQR 0.437–0.490, range −0.002 to 0.613, n = 9 benchmark-arms)." |
| "Six MATCHED, nine PARTIAL, three NOT MATCHED" as the audit's headline. | It discards the information the continuous statistic exists to carry, and it is threshold-dependent: applied mechanically the same rule flips PI-CAI from NOT MATCHED to PARTIAL while the fraction does not move. (`audit_results.md` §1) | Lead with the distribution; keep the verdict as a secondary column and report both PI-CAI readings. |
| "Roughly half of published medical-imaging performance is trivial." | A trivial fraction of 0.47 is half of the *margin over chance*, not half of the number, and it is not a decomposition. | "About half of the margin over chance that these published evaluations certify is also reachable by a model with no access to the pixels." |
| "Two benchmarks resisting the null shows the measure is specific." | n = 2 out of 9 rows. That is an existence proof, not a false-positive rate. (`audit_results.md` §4bis.1) | "Two of nine peer-reviewed head-to-heads returned a fraction at or below zero, which demonstrates the measure can fail to fire; no estimate of its specificity is claimed." |
| "Rempe et al.'s model learned nothing." | We could not reproduce their pipeline: their protocol on our prostate DWI cache gives 0.616 against their reported 0.809 (`pipeline_out/s12_waterfall_magphase.log:431`), and 0.574 against 0.813 (`s12_arm_mag.log:131`). A label file cannot support a claim about a model's internals. | "Their *evaluation protocol* certifies a number that a pixel-blind model also reaches." Repeat verbatim in abstract, results, discussion and every figure legend. |
| "We discovered that slice position confounds slice-level evaluation." | Known. Geirhos 2020; Yagis 2021; Wen 2020; Badgeley 2019; Kapoor & Narayanan 2023; and for position specifically Yan et al. CVPR 2018 already published a location-only baseline. | "Known in principle, unquantified in practice under a correct split. We measure how much of a published number it accounts for, and release the means to check." |
| "Published medical imaging benchmarks are broadly invalid." | Two of nine peer-reviewed head-to-heads returned a trivial fraction at or below zero, and the other seven sit near 0.5, not near 1. PI-CAI is a benchmark doing it right. | "Benchmarks differ, and the difference is measurable in one command." |
| "Deep learning on medical images is no better than trivial baselines." | Not tested. We never trained a competitive model on any audited benchmark. | Nothing. Delete the sentence. |
| "Phase carries no diagnostic information in MRI." | Our three clinical cohorts are 45–70 subjects, single-institution, single-vendor, 3 T. A null on 67 patients is a null on 67 patients. | "In our cohorts, under a pre-registered nine-criterion protocol, the hypothesis was NOT SUPPORTED on 6–7 of 9 criteria in all three." |
| "The audit covers benchmarks we do not hold." | True for five targets, false for fastMRI+ knee (needs HDF5 headers, `audit_results.md` §3.3) and false for RSNA ICH (`§6`). | "Five of seven label tables were sufficient on their own; two were not, and the failure of one of them is itself a finding about release practice." |
| "The trivial fraction decomposes the published number." | It is a ratio of two margins, not a decomposition. The baseline and the model may exploit the same, different or overlapping shortcuts. (`protocol.md` Rule 7 limits) | "It bounds the part of a reported margin reachable without pixels." |
| "The zero-image baseline's CI shows the published number is not significantly better." | The interval propagates uncertainty in the **baseline only**; the published number enters as a constant. | State the limitation in the same sentence as the fraction, every time. |
| "No published paper reports a zero-image baseline." | The screen sampled 250 papers and read 91. Absence in a sample of 91 is not absence in a literature of 9,979. And Yan et al. CVPR 2018 *does* report a position-only baseline — it is simply outside the frame's date and modality window. (`FINDINGS.md` §1.5, §8) | "In a pre-registered random sample, 0 of 91 readable papers reported one (95% CI [0.0%, 4.1%]; bounding interval [0.0%, 32.6%]), and no positive code appears on any of the four sub-flags in any of 345 coded records." |
| "0% of the literature reports a trivial baseline." | 32.6% of the eligible sample was unreachable, above the 15% threshold at which our own protocol makes the bounding interval the headline — and four independent blocks put it between 22.7% and 35.6%, so the extension could not fix it. (`FINDINGS.md` §1.1) | Always give the bound: "between 0.0% and 32.6%". Never the point estimate alone. |
| "Only 32% of papers split at the patient level." | Four screeners agreed on `split_unit` at κ = 0.637 [0.430, 0.824] even after the codebook amendment. The field is not reliably codeable. (`FINDINGS.md` §1.2, §3.6) | "31.9% [23.2%, 42.0%] stated a subject-level split, at inter-rater κ = 0.64 — the papers are frequently not written clearly enough to code this field." |
| "Our screen had good inter-rater reliability." | It failed its own pre-specified floor on the primary field as sealed: raw 65.6% against a 90% threshold, Fleiss' κ −0.015 against 0.60. The remedy has now run and the floor is met (raw 95.6%, κ 0.932), but on a **re-encoding of the same four sealed files**, and every reserve record is single-coded. (`FINDINGS.md` §1.2, §4) | Report the failure, then the diagnosis (a codebook with no "not assessable" level), then the restricted 100%, then the post-amendment κ **with the words "the same four sealed files" in the same sentence**, then that a fresh independent four-screener re-code under v1.2 has not been run. In that order. |
| "The prevalence screen is a systematic review." | It is a pre-registered random-sample prevalence screen, n = 91 included against a pre-registered target of 75, with the extension rule executed to its stopping point. It is not exhaustive and does not claim to be. | "A pre-registered prevalence screen of a random sample from a frozen 9,979-record frame, with the pre-specified extension rule run to its stopping point at permutation position 260." |
| "The evaluation unit changes which method wins." | Zero of 447 method pairs survive; between-unit rank disagreement exceeds within-unit resampling noise in none of the five cohorts. (`rank_inversion.md` §3) | "Between-unit rank disagreement did not exceed the within-unit disagreement produced by resampling subjects, and no inversion pair survived correction; at these cohort sizes the ranking does not reproduce across two disjoint halves of the subjects at either unit." |
| "Slice-level benchmarks select the wrong architecture." | Nothing anywhere establishes that a slice-level winner is *wrong*, because no located two-unit comparison — ours or published — identifies a winner at either unit. (`rank_inversion.md` §6) | "Published orderings do change with the unit; no two-unit comparison yet reported, including ours, shows the change exceeds sampling noise." |
| "Prostate T2 and DWI share none of their top-three methods between units, so the unit reorders methods." | Both are chance reorderings; the results file labels them as such. Prostate T2's patient-level split-half τ is −0.42 and all 21 of its patient-level AUROCs lie in 0.381–0.539. | Quote the split-half τ in the same sentence as the disjoint top-3 sets, or do not quote the top-3 sets. |
| "The unit × condition interaction shows the unit changes the winner." | The difference-of-differences is supported; each individual ordering is a tie (e.g. densenet121 phase vs magnitude: +0.004 [−0.041, 0.046] slice, −0.023 [−0.078, 0.027] patient). | "Aggregating to the patient unit shifts the magnitude-versus-phase contrast by +0.028 AUROC [0.015, 0.041] and reverses its sign for two of six architectures, though neither ordering is individually distinguishable from a tie." |
| "Our 21 methods are 21 methods." | They are 7 architectures × 3 input conditions, one pipeline, one seed, trained by us. | "21 method configurations from our own architecture zoo, which have far less between-method spread than a challenge leaderboard; this arm is an internal consistency check, not evidence about the field." |

---

## 3. Venue recommendation

### 3.1 What kind of paper this actually is

Weigh the three candidate framings the brief names:

* **Methods/tools article.** Fits. There is a tool (`trivialbaselines/`, numpy + pandas,
  MIT, self-tested), a protocol (`paper/protocol.md`, seven rules, each with a measured
  failure behind it), a checklist (`paper/checklist.md`), and a worked example. The
  weakness is that the tool is not hard — it is ~100 lines of statistics plus careful
  bookkeeping — so the contribution has to be the *discipline*, not the code.
* **Benchmark audit.** Fits partially. Seven benchmarks is a respectable N for an audit, and
  four now produce a defensible *peer-reviewed* comparison on the same metric and unit (RSNA
  ICH, LUNA16, DeepLesion, PI-CAI). None of those four is matched, which is why the audit is
  reported as a distribution of trivial fractions rather than as a count of matches; on that
  reading it is a real measurement rather than a thin one. The one benchmark that *is* matched
  rests on a preprint and is demoted to a worked example.
* **Perspective with an empirical core.** Fits the material well, and would let the paper
  lead with "here is what a benchmark should publish" and use the audit as evidence. But
  perspectives are usually invited, do not carry a software release well, and would waste
  the strongest asset — the fact that every number here is reproducible from a public CSV.

**Recommendation: write it as a methods/tools article with an audit as its evidence base.**
Title the audit honestly (seven benchmarks, eight label files, 24 comparator rows, median
trivial fraction 0.469 against peer-reviewed numbers, zero matched against peer-reviewed
numbers) and
put the protocol and the tool in the abstract. That framing is the only one where the two
NOT MATCHED benchmarks are an asset rather than an embarrassment.

### 3.2 Ranked venues

**1. Radiology: Artificial Intelligence (RSNA). Impact factor ~9–10. RECOMMENDED PRIMARY.**

Reasons:
- The audience is exactly the population whose behaviour the checklist is meant to change:
  radiologists and medical-imaging ML researchers who publish slice-level AUROCs.
- RSNA has an institutional appetite for reporting standards (CLAIM, and the RSNA dataset
  papers cited in `audit_targets.md` §1.7, §1.8). A checklist is a *native artefact* there.
- Five of the eight label files audited come from datasets RSNA readers use directly, and
  one of them is RSNA's own (**RSNA 2019 Intracranial Haemorrhage**, plus fastMRI Prostate,
  fastMRI+, DeepLesion, LUNA16/LIDC). The flagship result is computed on an RSNA challenge
  file and set against the RSNA organisers' own on-record statement about that file.
- The closest prior art is **not** in this journal, so the "you already published this"
  desk rejection is much less likely.
- The two NOT MATCHED benchmarks read as rigour to this audience rather than as a weak
  result.

Risk: reviewers will be clinically literate and will ask the anatomy question (see §8.3).
That is answerable, and §7 of `COLLABORATORS.md` asks for exactly that answer.

**2. npj Digital Medicine. Impact factor ~12–15. HIGHER IMPACT, HIGHER RISK. Send a
pre-submission enquiry before writing to their format.**

Reasons for:
- Best topical fit of any journal. It published Badgeley et al. 2019 (`10.1038/s41746-019-0105-1`)
  and Ong Ly et al. 2024, the two closest works to ours, and Varoquaux & Cheplygina 2022.
  The editors already believe the problem matters.
- The "audit without the pixels" property is exactly the kind of scalable-governance point
  that journal likes.

Reason against, stated plainly:
- **Ong Ly et al. 2024 is a direct competitor and it is in this journal.** They report that
  across 13 datasets performance is overestimated by up to 20% on average due to shortcut
  learning of acquisition biases, and they release an estimator (PEst). An editor can
  reasonably say "we ran this in 2024". Our differentiators — zero-image so no pixel access
  is needed, the slice-versus-patient unit collapse which PEst does not address, and the
  positional null specifically — are real but must be argued in the cover letter, not
  discovered by the editor.
- Mitigation: a 200-word pre-submission enquiry that names Ong Ly 2024 in the first
  sentence and states the three differentiators. If the editor is cold, go to Radiology: AI
  immediately and do not spend a submission cycle finding out.

**REVISED 2026-07-29 — this enquiry is now materially more likely to land, and the enquiry
should lead with the screen, not the audit.** npj Digital Medicine publishes field-level
evidence about how medical AI is evaluated; that is what Badgeley 2019 and Ong Ly 2024 are.
The prevalence screen is precisely that kind of object and it is **the one thing Ong Ly et al.
do not have**: they measure how much shortcut learning inflates performance, we measure how
often anyone checks. A 200-word enquiry that opens *"In a pre-registered screen of 100
randomly sampled volumetric-imaging classification papers coded by four independent
screeners, none of the 35 readable papers reported a measured zero-image baseline and none
reported the positional distribution of its labels"* is a stronger first sentence than
anything the audit can supply, and it does not touch the preprint exposure at all.

**UPDATED 2026-07-29 with the final pooled screen — the enquiry's opening sentence is now
this one, and it is stronger still because it needs no denominator:**

> *In a pre-registered random-sample screen of the PubMed volumetric-imaging classification
> literature — 250 papers drawn in permutation order from a frozen 9,979-record frame, 345
> independently coded records over 300 papers — not one record carries a positive code on any
> of the four zero-image baselines we pre-specified: constant/prevalence, positional,
> acquisition-metadata, permuted-label. Among the 91 papers whose full text we could obtain,
> P1 = 0/91, 0.0% [0.0%, 4.1%]; because full text was unreachable for 32.6% of the eligible
> set, our protocol makes the bounding interval [0.0%, 32.6%] the headline, and we report it
> that way.*

Two things the enquiry must not do. It must not state 0% without the [0.0%, 32.6%] bound —
an editor who discovers the censoring later will discount everything else in the letter. And
it must not overstate what the screen covers: the pre-registered target of 75 included papers
is **met** at n = 91 and the extension rule ran to its stopping point, so say that, but say in
the same breath that 32.6% of the eligible set was never read and that this is a property of
the literature's access conditions rather than of the sample size — four separately screened
blocks returned 35.6%, 34.6%, 22.7% and 32.1%. An editor who is interested will ask, and the
answer is better volunteered.

**A third thing the enquiry must not do, added 2026-07-29:** it must not quote the
post-amendment κ of 0.932 without saying it is a re-encoding of the same four sealed files. A
methods-literate editor at this journal will spot the difference between a re-encoding and a
re-rating, and being the one to point it out is worth more than the number is.

**Fourth differentiator to add to the letter:** the audit runs from *published label files
only*, so it can be applied to benchmarks the authors will never hold — which is the property
that makes it scalable governance rather than a case study. PEst requires pixel access.

**3. Medical Image Analysis (IF ~11) or MELBA.** Methods audience, tool-friendly, and the
reviewers will scrutinise the statistics harder than anyone else — which will improve the
paper. Lower clinical reach, so the checklist lands with fewer of the people who need it.
Good third choice, and a good *first* choice if the biostatistics review (§`COLLABORATORS.md`
§2) turns up problems that need a specialist referee.

**4. Patterns (Cell Press, IF ~7).** Published Kapoor & Narayanan 2023. Would take the
"leakage + released instrument" framing happily. Lower impact, near-certain acceptance.

**5. Nature Machine Intelligence / Nature Medicine / Lancet Digital Health / Nature
Biomedical Engineering. NOT ON THIS EVIDENCE.** Sending there now costs 3–6 months and
returns a desk rejection whose stated reason will be "the phenomenon is established and the
new evidence rests on a single benchmark". That reason would be correct.

### 3.3 What would make it an IF 15+ paper — SCORED 2026-07-29

The four preconditions were named before the screen and the audit revision ran. Here is
what each one actually returned. **Two of four landed. Is IF 15+ now plausible? No — but
the reason has moved, and that matters for what to do next.**

| # | precondition, as written | outcome | status |
|---|---|---|---|
| 1 | **Three or more independent benchmarks MATCHED against peer-reviewed numbers on the same metric and unit** | Valid peer-reviewed comparisons went **3 → 4** (RSNA ICH added). Benchmarks *matched* against a peer-reviewed number: **zero**. All six MATCHED rows still rest on arXiv:2407.06165. | **FAILED, and it is the binding constraint** |
| 2 | **A structured prevalence screen converting "here is a failure mode" into "here is its prevalence"** | Delivered, pre-registered, four independent screeners on the overlap set, extension rule run to its stopping point: 250 papers screened, 91 included. **0/91 report any zero-image baseline (bound [0.0%, 32.6%]); 1/91 report the positional distribution of labels; 0 positive codes in 345 records over 300 papers.** Target of 75 MET. Agreement remedy RUN and both floors now met. **One caveat remains and it is the binding one: 32.6% censoring, which four independent blocks show is not a sample-size problem.** | **LANDED; two of three caveats cleared** |
| 3 | **Rempe et al. peer-reviewed, or a peer-reviewed anchor in its place** | Re-queried on the arXiv API 2026-07-29: no journal-ref, no DOI, no Europe PMC record, two years after posting. | **FAILED** |
| 4 | **A clean prior-art re-audit** | Went the *wrong* way. Yan et al., CVPR 2018 Table 1 publishes a position-only baseline in DeepLesion's own defining paper (59.7% vs 90.5%). The MICCAI/MIDL/ML4H sweep is still not done. | **FAILED so far; may get worse** |

**A fifth precondition was added and tested after the table above was written, and it also
failed.** It was the one with the largest tier upside.

| # | precondition, added 2026-07-29 | outcome | status |
|---|---|---|---|
| 5 | **A well-supported rank inversion: the evaluation unit changes which method a benchmark selects, demonstrated on our data or located in published work with intervals** | Tested with the trap test built in. **0 of 447 method pairs survive; between-unit rank disagreement exceeds within-unit resampling noise in 0 of 5 cohorts; 3 of 5 cohorts cannot rank at either unit (split-half τ = −0.42, −0.24, −1.00 at the patient unit).** Published inversions exist and change winners (CAMELYON16 τ = 0.754; Ruan 2021 τ_a = 0.603, winner changes), but the only two that publish intervals at both units have the inversion inside them. One narrower effect is supported: the paired unit × condition interaction on the brain confound cohort, +0.028 [0.015, 0.041], p = 0.001, 4/6 Holm-supported, 2 sign flips, individual orderings all ties. | **FAILED as a tier-mover; LANDED as a reportable null.** `paper/rank_inversion.md` |

**The honest revised call: IF 15+ is still not plausible, and should not be attempted.**

Why, in one sentence a hostile editor would write for us: *the paper's flagship quantitative
claim — that a pixel-blind model matches a published number — rests on a single unreviewed
preprint, and the field-level claim that would carry the paper on its own leaves a third of
the eligible sample unread, so its headline is a 33-point-wide bound rather than an estimate.*
**REVISED 2026-07-29:** the second half of that sentence is now half-fixed — n = 91 beats the
pre-registered target of 75 and the agreement floor is met — but the censoring clause survives
verbatim, and it survives *because* the extension proved censoring is not a sample-size
problem. Both halves remain fixable. Neither is fixed.

**What did change, and it is not nothing.** Before the screen, the paper was a methods note
with a preprint-anchored headline. After it, the paper has an independent, pre-registered,
field-level empirical result that stands whether or not any benchmark is ever matched:
**the check is not merely under-reported, it is absent — in 91 of 91 readable papers, and in
345 of 345 coded records over 300 distinct papers.** That moves the paper from the *bottom* of
the Radiology: AI band to the *top* of it, and it makes the npj Digital Medicine enquiry a real
prospect rather than a courtesy. It does not reach IF 15+.

**Revised order of work, by tier-per-hour.** Items 1 and 3 need data access, an accepted
click-through agreement, or someone else's editor; items A–C below need only labour and would
remove all three caveats on the one thing that did land:

- ~~**A. Execute the extension rule.**~~ **DONE 2026-07-29.** Reserve blocks R1–R3
  (permutation positions 111–260) took the screen from 35 → **91** included papers and the
  zero-count upper bound from 9.9% to **4.1%** — better than the 4.9% the protocol targeted.
  `FINDINGS.md` §3.1.
- ~~**B. Run the triggered adjudication remedy.**~~ **DONE 2026-07-29.** Codebook amended with
  rules D1–D14 including the missing `not_assessable` level, all 145 already-coded records
  re-coded, pre- and post-reconciliation statistics reported. Both floors now met (raw 95.6%,
  κ 0.932). **Residual: the post-amendment number is a re-encoding of the same four sealed
  files, and a fresh four-screener re-code under v1.2 has not been run.** `FINDINGS.md` §1.2.
- **C. Attack the censoring. THIS IS NOW THE ONLY LEVER ON THE HEADLINE.** 44 unreachable full
  texts. One screener with institutional access, the Europe PMC render-PDF rung applied to all
  44, or protocol ladder rung 5 (author requests, 21-day clock). The extension proved that
  screening more papers cannot help: four blocks, unreachable rates 35.6 / 34.6 / 22.7 / 32.1%.
  Getting below 15% promotes the complete-case 0/91 = 0.0% [0.0%, 4.1%] to the headline and
  turns the paper's central field-level claim from a bound into an estimate. `FINDINGS.md` §1.1.

A and B have landed. **If C lands**, the reportable P1 becomes "0 of 91, upper bound 4.1%, κ
adjudicated and floor met" and the paper is worth re-testing against npj Digital Medicine and
Nature Machine Intelligence. If item 1 *also* lands — a peer-reviewed MATCHED row on any
benchmark — revisit IF 15+. Until then, do not.

### 3.4 The venue call after the rank-inversion null — the straight answer

Asked repeatedly, so answered once, without hedging.

**The rank-inversion arm was the highest-upside item in the project and it returned a null.**
There is no well-supported inversion in our data and no published inversion with non-overlapping
intervals at both units. Therefore:

* **Radiology: Artificial Intelligence remains the primary target, unchanged.** The null does
  not weaken the paper — it adds a properly powered negative result, a literature denominator
  (1 of 2,642 scanned full texts reports both units for ≥ 2 methods; 7 of 350
  benchmark-anchored papers do) and a defensible synthesis sentence. It supplies nothing that
  would justify moving up.
* **npj Digital Medicine stays a shot, not a target.** Send the enquiry
  (`paper/enquiry_npj.md`), still led by the prevalence screen. Add the rank result as **one
  clause**, phrased as the null: *"and we show that at realistic cohort sizes the ranking is not
  identified at either unit, so the unit question cannot be settled by single-centre data."*
  That clause is honest, it pre-empts the obvious reviewer question, and it is not a claim an
  editor will mistake for the paper's core. Do not lead with it and do not imply an inversion.
* **What would have changed the call, recorded so it is not re-litigated:** a Holm-surviving
  inversion pair on a clinical cohort, or a published inversion whose intervals at the two
  units do not overlap. Neither exists. The closest published case (Jarkman 2022, a near-complete
  reversal on CAMELYON16 with CIs at both units) has intervals that overlap almost completely.
* **The one route that could still change it — and it is now the highest tier-per-hour item
  left.** PI-CAI ranks its 293 submissions by `(lesion-level AP + patient-level AUROC)/2` on a
  hidden 1,000-scan cohort. That is a large, independent, two-unit ranking with published
  intervals, in the same organ as two of our cohorts, and it would settle the question in
  either direction. The per-algorithm table is on a JavaScript leaderboard that could not be
  read and the evaluation API returns nothing without authentication. **Action: email the
  PI-CAI organisers for the per-algorithm AP and AUROC table, or read the leaderboard in a real
  browser.** One email and a fortnight. If it yields a between-unit inversion that clears a
  within-unit noise floor on 293 independent submissions, re-test npj Digital Medicine and
  Nature Machine Intelligence. Second-best route: re-run three or more published systems'
  released weights on one cohort and score both units ourselves.
* **What must not be done:** re-running the rank analysis with more seeds, more architectures or
  a different aggregation rule in the hope of a different answer. The noise floor rises with the
  seeds and the multiplicity family grows with the methods; both changes make the null more
  secure, not less. The binding constraint is that our methods are our own configurations, and
  only outside data fixes it.

---

## 4. Title

Recommended:

> **What a slice-level benchmark certifies without the pixels: a label-file audit of seven
> public medical imaging benchmarks**

Runner-up (leads on the remedy, better for Radiology: AI):

> **Slice-level AUROC certifies stack geometry: pixel-blind baselines, a
> position-stratified remedy, and an audit of seven public benchmarks**

Reject: *"Trivial baselines match published performance on medical imaging benchmarks."*
It over-claims on this evidence, and `audit_results.md` §8.1 says so already.

---

## 5. Section-by-section outline, with every number and its source

Every numeric claim in the draft must appear in this table with a file path. If it is not
here, it does not go in the paper.

### 5.1 Abstract (~250 words)

Order the abstract's results in this order: **the collapse, then the distribution, then the
exceptions, then the worked example.** The preprint must not appear before the third
sentence of the results, and nothing before it may depend on it.

| claim | number | source |
|---|---|---|
| **the collapse (lead)** | RSNA ICH, 752,802 slices / 18,938 patients: **0.737 [0.735, 0.740] slice → 0.453 [0.445, 0.461] patient**, same score vector; max-agg 0.500; all six labels, gaps 0.205–0.307 | `pipeline_out/trivial_baselines/rsna_ich_unit_collapse.json` |
| collapse, corroborating arms | 0.854 → 0.506; 0.851 → 0.424; 0.873 → 0.510 | `fastmri_prostate_{t2,dwi}_published.md`, `fastmriplus_knee_meniscus_tear.md` |
| **the distribution (second)** | peer-reviewed comparators, strongest system per benchmark-arm: median **0.469**, IQR 0.437–0.490, range −0.002 to 0.613, n = 9; RSNA ICH subtypes 0.395–0.613 | `paper/trivial_fraction_distribution.{json,md}` |
| benchmarks audited | 7 benchmarks / 8 label files / **24 (benchmark, comparator) rows** | `paper/audit_results.md` §0, §2 |
| verdict distribution (secondary) | 6 MATCHED, 17 PARTIAL, 1 NOT MATCHED; **0 MATCHED among peer-reviewed comparators** | `audit_results.md` §2.0–2.1 |
| the exceptions | LUNA16 trivial fraction **−0.002** (CPM 0.0020 vs random-score 0.0027); PI-CAI positional exactly 0.500; DeepLesion 0.977 / 0.954 at both units | `luna16_fp_reduction_candidates.md`, `picai_case_level.md`, `deeplesion_pelvis_vs_rest.md` |
| the worked example, labelled a preprint | 0.854 [0.812, 0.891] vs published 0.861 | `fastmri_prostate_t2_published.md` |
| the remedy | 0.854 → 0.546; 0.851 → 0.539 | `pipeline_out/rempe/positional_baseline.json`, `..._dwi_labels.json` |
| the tool | numpy + pandas only, MIT | `trivialbaselines/pyproject.toml` |

### 5.2 Introduction (~900 words)

Four paragraphs, in this order. The order matters: prior art comes **second**, not in a
"related work" section at the end, because the reviewer's first question is "what is new".

1. **The setup.** 3D acquisitions labelled per slice; performance pooled over slices; the
   clinical question is about a patient. State the arithmetic problem in two sentences.
2. **What is already known, cited generously and against our own interest.** Shortcut
   learning (Geirhos 2020); acquisition and process metadata (Badgeley 2019 — fracture
   AUC 0.78 from image, 0.91 with hospital process features, 0.52 on a balanced test set,
   scanner model predictable at AUC 1.00; `audit_targets.md` §3.1); slice-level split
   inflation (Yagis 2021 — 30%/29%/48%/55%, and ~96% accuracy on **randomly labelled** data;
   Tampu 2022; Wen 2020, in which CNNs "did not perform better than a SVM with voxel-based
   features"); leakage as a cross-disciplinary failure (Kapoor & Narayanan 2023, 294 papers
   across 17 disciplines); field-level critiques (Varoquaux & Cheplygina 2022; Roberts 2021);
   and **a published position-only baseline** (Yan et al., CVPR 2018, Table 1, "Baseline:
   Location feature", 59.7% against their 90.5%). This paragraph must be longer than a
   reviewer expects. It is the single best protection against a redundancy rejection.
3. **The gap.** Prior work measures the cost of a *wrong* split. We ask a different
   question: under a **correct, patient-disjoint** split, how much of a published
   slice-level number is reachable from the label file alone? Rempe et al.'s split *is*
   patient-disjoint (`audit_results.md` §3.1), so our 0.854 is not a leakage result in the
   Yagis sense. Say this explicitly or a reviewer will conflate the two and reject for
   redundancy (`audit_targets.md` §3.2.1).
4. **What this paper does.** Three deliverables: a family of pixel-blind nulls that need
   only four columns (subject id, slice index, label, split); an audit of seven benchmarks
   reporting what fires *and* what does not, as a continuous measure; a remedy metric and a
   one-page checklist. State the headline **distribution of trivial fractions** in the
   introduction, not only in the results — including the two rows at the floor — because a
   paper that hides its negative rows until page 9 reads like advocacy.

### 5.3 Methods (~1,600 words)

**5.3.1 The zero-image family.** Five baselines, all `fit(train)` / `score(test)`:
`prevalence`, `positional_20bin`, `volume_size`, `metadata_tree` (depth-limited CART on
acquisition/administrative columns), `combined_position_metadata`
(`trivialbaselines/README.md`, "The baselines"). Relative position is
`(slice − min_slice_in_volume) / (max − min)`.

**5.3.2 Column discipline.** Outcome-derived columns (the label under another name) and
image-derived columns (they break the zero-image premise) are excluded by default, by a
fallible name heuristic, and every included and excluded column is written to the JSON.
Concrete instance to report in the text: on PI-CAI, `prostate_volume` and `psad` were
excluded because they are measured *from the MRI*, and `case_ISUP`, `lesion_ISUP`,
`lesion_GS`, `lesion_PIRADS`, `histopath_type` were excluded as outcome-derived
(`audit_results.md` §3.5). Naming the exclusions is what makes the guarantee checkable.

**5.3.3 Evaluation.** Both units always, from one score vector. Subject-clustered
percentile bootstrap, 2,000 replicates, seed recorded. Degenerate replicates counted, not
dropped silently. The naive slice-level interval is computed too and reported *as the wrong
one*, so the width difference is visible.

Coverage, measured on simulated data with a closed-form truth
(Φ(μ/√(2σ²ᵤ+2σ²ₑ)) = 0.6880; 200 datasets × 20 patients × 15 slices, 500 bootstrap
replicates each): subject-clustered **91.5%** coverage, mean width 0.370; naive slice-level
**46.5%**, mean width 0.117; ratio **3.18×**.
*Source: `python pipeline/s04_stats.py --self-test`, block [6], re-run and confirmed
2026-07-29.*

**5.3.4 The trivial fraction and the verdict rule.**
`(best zero-image baseline − chance) / (published − chance)`, chance = 0.5 for AUROC and
the majority-class rate for multi-class accuracy. Verdict rule stated **before** any result
(`audit_results.md` §1): MATCHED if the baseline's clustered 95% upper bound reaches the
published number; PARTIAL if fraction ≥ 0.30 with CI wholly below 1; NOT MATCHED if
fraction < 0.30 or the baseline is indistinguishable from chance; NON-COMPARABLE if the
published number is on a different cohort, split, label or metric.

**5.3.5 The remedy: position-stratified AUROC.** Mann–Whitney within bins of relative
position, so only same-position positive/negative pairs contribute
(`trivialbaselines.stratified_auc`). This removes exactly the share of a slice-level AUROC
that stack geometry paid for, and nothing else.

**5.3.6 Each baseline's own permutation null.** Not automatically 0.5. An out-of-fold
metadata model on a subject-level label sits systematically *below* chance because the rate
fitted is anti-correlated with the rate scored; on a synthetic dataset whose label is by
construction invisible to metadata it measures **0.424**. Judged against 0.5 that is a
manufactured below-chance "finding"; judged against its own null it is correctly reported as
no effect. *Source: `trivialbaselines --self-test`; `protocol.md` Rule 4.*

**5.3.7 Benchmark selection.** State the entry criterion (four columns obtainable without
pixel download and without a DUA covering pixels) and report the exclusions with reasons —
CQ500 (scan-level labels only), BraTS/KiTS/MSD/AMOS/TotalSegmentator (segmentation metrics,
masks shipped with images), PROSTATEx (needs DICOM headers), MRNet (exam-level)
(`audit_targets.md` Tier 3). The exclusions are as load-bearing as the inclusions.

**5.3.8 Provenance table.** Every label file with bytes, SHA-256 prefix, source URL and
licence (`audit_results.md` §6). This is Table 1.

**5.3.9 The worked-example cohorts.** The k-space phase study: 3 clinical cohorts
(prostate T2 n=67, prostate DWI n=45, breast n=70), 2 confound cohorts (brain n=454, knee
n=96), 102 training runs (`pipeline_out/results`, 103 JSONs of which one is
`statistics.json`), 456 control runs (`pipeline_out/controls`), 5-fold subject-level CV,
2 seeds. Reconstruction validated against the vendor references shipped in the same HDF5:
brain r = 1.000 (2,270 slices / 454 files), knee r = 1.000 (995 / 199), prostate T2
r = 0.9982 (2,039 / 67), prostate DWI **per-file low-b magnitude-averaged r = 0.9835**
(per cached slice 0.8866, and the reason for the gap is stated), breast r = 0.9772 —
**and the breast reference is not ground truth**: `temptv` is the vendor's temporal-TV
*regularised* reconstruction of the same radial k-space, so that r is agreement between two
estimators. *Source: `pipeline_out/recon_fidelity/recon_fidelity_summary.json` and
`run_streamA.log:160-206`.* Report the breast caveat in the main text, not a footnote.

**5.3.10 Software.** `trivialbaselines` v1.0, MIT, `numpy` + `pandas` only; console script
`trivial-baselines`; `--self-test` with known answers; every run writes a JSON payload and a
markdown card. **Before submission, either publish to PyPI or change every
`pip install trivialbaselines` in `protocol.md` and `checklist.md` to the
`git clone && pip install .` form that `trivialbaselines/README.md` actually uses.** Right
now those two documents disagree, and a reviewer who tries the command in the protocol will
get a 404.

### 5.4 Results (~2,200 words)

**R0. The flagship, first, before any published number appears in the paper.**
RSNA ICH, 752,802 slices / 21,744 series / **18,938 patients**; five-fold subject-disjoint CV
pooled out of fold; one pixel-blind score vector read at two units.

| quantity | value | source |
|---|---|---|
| **slice AUROC, `any`** | **0.7374 [0.7351, 0.7398]** | `rsna_ich_unit_collapse.json` |
| **patient AUROC, `any`** (mean-agg) | **0.4533 [0.4454, 0.4613]** | same |
| patient AUROC, max-agg | 0.5001 | same |
| gap | 0.284 | same |
| the same on all six labels | 0.690–0.805 slice, 0.453–0.497 patient, gaps 0.205–0.307 | same |
| within-series permutation null | 0.502 slice / 0.523 patient | same |
| constant predictor | 0.492 slice / 0.501 patient (0.498/0.500 in the released tool) | same, `rsna_ich_any_slice_full.json` |
| naive slice-resampled interval | 1.5–2.0× too narrow | same |
| released tool, same file | 0.7376 [0.7352, 0.7399] / 0.4561 [0.4478, 0.4640] | `rsna_ich_any_slice_full.json` |
| bin sweep, slice / patient | 0.716/0.437, 0.733/0.445, 0.738/0.456, 0.745/**0.632** | same |
| no-fit centrality −\|relpos − 0.5\| | 0.735 | same |
| apparent (training) slice AUROC | 0.7379 vs held-out 0.7376 | same |

→ **Table 3 Panel A**, **Figure 2**. The 50-bin patient value (0.632) must be quoted with the
sweep: at 50 bins over 20–60-slice volumes the aggregate starts tracking volume length, which
is separately and weakly predictive (0.591 patient AUROC alone). Twenty bins is
pre-specified.

**R1. The audit, stated as a distribution before any individual row.**
7 benchmarks, 8 label files, **24 (benchmark, published comparator) rows**. Report the
distribution of the trivial fraction, not a verdict count. Primary line: peer-reviewed
comparator, strongest system per benchmark-arm — n = 9, median **0.469**, IQR 0.437–0.490,
range −0.002 to 0.613, with 8 of 9 in 0.30–0.70 and 1 at the floor. Secondary lines: all 24
rows (median 0.512, max 1.655); preprint comparator only (n = 6, median 1.142, the only rows
≥ 1). Verdict counts kept as a secondary summary: 6 MATCHED / 17 PARTIAL / 1 NOT MATCHED
overall, **0 MATCHED among peer-reviewed comparators**. *Source:
`paper/trivial_fraction_distribution.{json,md}`; `audit_results.md` §0.2, §2.0.* →
**Table 2**, **Figure 4**.

Report with it: the definition's behaviour at the extremes (undefined at or below chance;
unclipped above 1; negative below chance; unstable at a small denominator — 4.76 at a headroom
of 0.021, though the smallest denominator in this audit is 0.214), and the fact that the
mechanical verdict rule disagrees with the earlier hand-assignment on the two PI-CAI rows
while the fraction does not move. *Source: `audit_results.md` §4bis.*

**R2. fastMRI Prostate — the worked example, whose comparator is a preprint.**
Label it as such in the subsection heading and in its first sentence. Nothing in the abstract
or the conclusion may depend on it.

| quantity | value | source |
|---|---|---|
| published headline (image + k-space) | 0.861 ± 1.8 slice AUROC | Rempe et al. Table II, transcribed at `pipeline/s12_rempe.py::REPORTED` |
| published PCA ×2 magnitude + phase | 0.809 ± 2.1 | same |
| published PCA ×2 magnitude | 0.813 ± 2.2 | same |
| published R=16 PCA | 0.714 ± 2.9 | same |
| **T2 positional 20-bin, slice** | **0.854 [0.812, 0.891]** | `fastmri_prostate_t2_published.md` |
| T2 positional 20-bin, patient | 0.506 [0.381, 0.632] | same |
| **DWI positional 20-bin, slice** | **0.851 [0.816, 0.887]** | `fastmri_prostate_dwi_published.md` |
| DWI positional 20-bin, patient | 0.424 [0.298, 0.547] | same |
| trivial fraction vs 0.861 | T2 0.981 [0.865, 1.084]; DWI 0.973 [0.876, 1.073] | both cards |
| trivial fraction vs 0.809 | T2 1.146 [1.011, 1.266]; DWI 1.137 [1.023, 1.253] | `audit_results.md` §2.1 |
| trivial fraction vs 0.714 | T2 1.655 [1.459, 1.829]; DWI 1.642 [1.478, 1.810] | same |
| test arm | T2 1,399 slices / 46 subjects / 68 positive slices / 20 positive subjects | card |
| training arm | 6,647 slices / 218 subjects (T2); 6,637 / 218 (DWI) | `positional_baseline*.json` |
| bin sweep, T2 | 5=0.835, 10=0.848, 20=0.854, 30=0.854, 50=0.856 | `positional_baseline.json` |
| **no-fit centrality**, T2 / DWI | 0.825 / 0.841 | same JSONs |

The no-fit centrality number deserves its own sentence: **−|relative position − 0.5|, which
uses no training data at all, reaches 0.841 on the DWI file against a published 0.861.**
That is the cheapest possible statement of the result and it is immune to any accusation of
fitting.

**Correction that must be made before submission.** `paper/audit_targets.json`'s
`anchor_correction` block asserts DWI is the correct arm. The evidence points the other
way: Rempe et al.'s abstract says "312 subject and a total of 9508 slices", and 9,508 is the
exact row count of `t2_slice_level_labels.csv` (DWI has 9,490). **T2 is the correct arm**;
the persisted `pipeline_out/rempe/positional_baseline.json` is already the T2 run and is
right; the docstring waterfall at `pipeline/s12_rempe.py:272-278` quotes the wrong arm.
Both arms are reported so the conclusion does not depend on it. *Source:
`audit_results.md` §3.1.*

**R3. The unit-of-evaluation collapse on the remaining benchmarks.** → **Table 3 Panel B**,
**Figure 2**. Every cell is our own computation on a published label file; no published number
enters. RSNA ICH leads this result and is in R0 above; these are the corroborating arms, and
their cohorts are 46–1,476 subjects against RSNA ICH's 18,938.

| dataset-arm | slice | patient |
|---|---|---|
| fastMRI Prostate T2 | 0.854 [0.812, 0.891] | 0.506 [0.381, 0.632] |
| fastMRI Prostate DWI | 0.851 [0.816, 0.887] | 0.424 [0.298, 0.547] |
| fastMRI+ knee, meniscus tear | 0.873 [0.858, 0.886] | 0.510 [0.428, 0.592] |
| fastMRI+ knee, any finding | 0.801 [0.779, 0.824] | 0.558 [0.470, 0.648] |
| Duke breast, owner slice task | 0.823 [0.811, 0.834] | **undefined** (922/922 patients positive) |
| DeepLesion, pelvis vs rest | 0.977 [0.969, 0.984] | 0.954 [0.939, 0.967] |
| LUNA16 candidates | 0.534 [0.513, 0.558] | 0.581 [0.538, 0.613] |
| PI-CAI, case level | not applicable (no slice index in the marksheet) | metadata only, 0.692 [0.626, 0.755] |

*Sources: the seven cards named in §5.1 plus `duke_breast_owner_slice_task.md`,
`fastmriplus_knee_any_finding.md`, `picai_case_level.md`; all reproduced in
`audit_results.md` §4.*

Report all eight rows. The two that do not collapse are what make the rest credible.

**R4. Two benchmarks that resist the null — equal prominence, not a footnote.**

*LUNA16.* Scored on the competition's own metric rather than on a convenient one:
`pipeline/audit_prep/luna16_cpm.py` runs the same 20-bin positional estimator through the
challenge performance metric — sensitivity at 1/8, 1/4, 1/2, 1, 2, 4, 8 FP/scan,
out-of-fold on a scan-disjoint 5-fold split. Result **CPM 0.0020**, sensitivity **0.0006**
at 1 FP/scan, against a random-score reference of 0.0027 and a published >0.95 sensitivity
at <1 FP/scan (Setio et al. 2017, arXiv:1612.08012). 754,975 candidate rows, 888 scans,
1,557 positives. The asterisk must travel with it: the FP-reduction track is conditioned on
`candidates_V2.csv`, produced by image-based detectors, so "zero-image" here means "zero
image *given the published candidate list*" (`audit_results.md` §3.6). Also report the
harness's own warning: the constant predictor scored 0.483, not 0.500, because pooling
out-of-fold across folds with different training prevalence makes fold identity rankable
(`luna16_fp_reduction_candidates.md`, Warnings).

*PI-CAI.* Published 0.91 (0.87–0.94) case-level AUROC for the AI system and 0.86
(0.83–0.89) for 62 radiologists reading PI-RADS 2.1 (Saha et al., *Lancet Oncol*
2024;25:879-887). Our best zero-image baseline, at the case level its authors report, is
**0.692 [0.626, 0.755]** (metadata CART); trivial fractions 0.467 [0.307, 0.622] and
0.532 [0.350, 0.708] — NOT MATCHED. The **positional** baseline is exactly **0.500** at
every bin setting, which is the correct registration of "inapplicable" (the marksheet has
one row per case and no slice index), not a computed result. Best single columns:
`patient_age` 0.639, `psa` 0.638. Cohort caveat, stated because it cuts against us and not
for us: the published numbers are on the hidden 1,000-case testing cohort; ours is on the
public 1,500-case Training and Development set with the benchmark's own official 5-fold
splits. A strict reading makes these rows non-comparable; they are scored anyway because our
baseline had the easier cohort and still lost. *Source: `audit_results.md` §3.5;
`picai_case_level.md`.*

**PI-CAI is the paper's positive example and should be labelled as such in the abstract:
a benchmark that evaluates at the unit it should, and has no slice-level number to attack.
Its metadata baseline is still 0.692, which is the point that fixing the unit does not fix
acquisition confounding — Rules 2 and 6 are independent.**

**R5. DeepLesion — where the positional null is the task, not a confound.**
Yan et al.'s conditions were reconstructed rather than assumed. Their Table 1 test set has
4,927 samples, which is *exactly* the row count of the official `Train_Val_Test == 3`
split — a coincidence that invites a false match. Their own text says otherwise: a random
patient-disjoint 25/25/50 partition of the type-labelled rows, fitting on the 25% seed set.
`pipeline/audit_prep/deeplesion_yan_conditions.py` rebuilds that partition over 200 draws
(mean seed 2,454 rows, mean test 4,900 against their 4,927). Zero-image accuracy **0.5571**,
sd 0.0131, [0.5243, 0.5778] over partitions, against a majority class of **0.2361**.
Trivial fractions: 0.480 [0.431, 0.511] against their 0.905 ± 0.002, 0.513 [0.460, 0.546]
against the 0.862 ImageNet-feature baseline, and **0.889 [0.799, 0.947] against their own
"Location feature" baseline of 0.597** — all PARTIAL.

Read conservatively, and say so in the text: the eight classes are *bone, abdomen,
mediastinum, liver, lung, kidney, soft tissue, pelvis* — anatomical regions. A z-coordinate
predicting an anatomical region is the task, not a confound. **This row is the reference
level a lesion-type classifier must clear. It is not evidence that DeepLesion papers are
unsound and the paper must not use it that way.**

One metadata finding worth its own sentence: on the official split, one-vs-rest for lung
lesions reaches slice AUROC **0.911** from the `DICOM_windows` header column alone
(`-1500, 500` for lung-reconstructed series, `-175, 275` otherwise); position alone gives
0.872; together 0.962 [0.949, 0.973]. Per-class zero-image ceilings on the official split:
pelvis 0.982, lung 0.962, mediastinum 0.957, kidney 0.896, abdomen 0.886, liver 0.876, bone
0.832, soft tissue 0.831. No published per-class AUROC was located, so these are reference
levels only. *Sources: `audit_results.md` §3.2; `deeplesion_*_vs_rest.md`.*

**R6. Metadata baselines beating a trained network — from the worked example, and labelled
as such.**

| cohort | field | field → label | trained phase network → label |
|---|---|---|---|
| breast (n=70) | `folder` — the release batch / download tarball, 7 levels | **0.743** | **0.633** (seed 42), 0.630 (seed 123) |
| prostate T2 (n=67) | `kspace_shape` — acquisition matrix, 14 levels | **0.609** | 0.483 (seed 42), 0.462 (seed 123) |

Both columns are subject-level AUROCs on the same subjects. In the breast cohort the field
also explains more of the *model's score variance* than the true label does: η² = 0.108 for
`folder` against 0.033 for the label (seed 42); 0.151 against 0.046 (seed 123). Which
tarball a scan arrived in has no causal relationship to whether the patient has cancer.
*Source: `python pipeline/s08_belowchance.py --cohort breast --condition phase`, re-run and
confirmed 2026-07-29; `pipeline_out/s08_belowchance.log` for prostate T2.*

**R7. The remedy.** Position-stratified AUROC on the same score vectors:

| | raw slice | position-stratified |
|---|---|---|
| zero-image positional, T2 label file | 0.854 | **0.546** (5 strata) |
| zero-image positional, DWI label file | 0.851 | **0.539** (6 strata) |
| our reimplementation, magnitude arm | 0.574 | **0.467** |
| our reimplementation, magnitude + phase arm | 0.616 | **0.562** |

*Sources: `pipeline_out/rempe/positional_baseline.json` and `..._dwi_labels.json`
(`slice_auc_position_stratified`); `pipeline_out/s12_arm_mag.log:134` and
`pipeline_out/s12_waterfall_magphase.log:434` (rung W4s).*
→ **Figure 5**.

**R8. What the label files could not support — reportable failures of release practice.**
RSNA 2019 ICH is the highest-impact target on the list and its **official competition metric
is per-image**. Its `stage_2_train.csv` is keyed by `ID_<SOPInstanceUID>_<subtype>` and
carries only the label: no patient id, no study id, no slice position. The join needs DICOM
headers from the ~450 GB image release or an unprovenanced third-party CSV. **A benchmark
whose official metric is per-slice publishes a label file from which the slice cannot be
located.** That sentence is the paper's argument for the three-field recommendation, and it
costs nothing to make. Access is additionally behind a click-through Research Use Agreement
that this analysis did not accept. *Source: `audit_results.md` §6.*
fastMRI+ is the other honest failure: it publishes positive annotations only, so slice
counts come from the fastMRI HDF5 headers. **Do not call fastMRI+ a label-file-only target.**
And the coverage is a 199-of-1,173-volume subset (17%), which is not the subset any
published number would use (`audit_results.md` §3.3).

**R9. Worked example: a study audited under its own protocol.** Kept short — 400 words in
the main text, the rest in a supplement. The point is that the protocol was applied to our
own work first and returned a null, not that MRI phase is or is not informative.

- Pre-registered primary cohort `prostate_t2` (n=67), chosen on size and on reconstruction
  validated at r = 0.998, both fixed before any verdict. **NOT SUPPORTED**, failing 7 of 9
  criteria (C1, C2, C3, C4, C5, C6, C8). `prostate_dwi` (n=45) NOT SUPPORTED on 7
  (C1, C2, C4, C5, C6, C7, C8). `breast` (n=70) NOT SUPPORTED on 6
  (C1, C2, C4, C5, C6, C7). *Source: `pipeline_out/report/RESULTS.md` §2.*
- Phase, patient-level, pooled out-of-fold: prostate T2 **0.380 [0.244, 0.530]**;
  prostate DWI 0.442 [0.254, 0.642]; breast 0.631 [0.457, 0.800]. Magnitude, prostate T2:
  0.473 [0.321, 0.632]. *`RESULTS.md` §7.*
- The permutation null on the primary cohort does not contain chance: observed range
  [0.548, 0.645] over 20 distinct replicates, so C3 fails and nothing downstream of it can
  be believed. *`RESULTS.md` §8.* Report this. It is the most damaging single fact about our
  own pipeline and burying it would be exactly the behaviour the paper criticises.
- Training on **air alone** (anatomy removed) does not collapse: prostate T2 0.604 against a
  headline of 0.629 (0.025 below, and the control's own CI [0.528, 0.673] lies entirely
  above chance); prostate DWI 0.595 against 0.586 — the control is **above** the headline;
  breast 0.549 against 0.587 (0.037 below). *`RESULTS.md` §8.*
  **Do not write "within 0.018".** 0.018 is the mean of the three gaps and no cohort
  attains it; quote the three numbers.
- Phase predicts receive-coil count at **0.921 [0.870, 0.966]** on 136 independent test
  subjects, against 0.913 [0.872, 0.950] for magnitude; the difference is +0.007
  [−0.038, +0.051] and includes zero, so **no ordering between the channels is claimed —
  the level is the point**. *`RESULTS.md` §4b.*
- Stratifying within site, phase still predicts the coil bucket at **0.979 [0.953, 0.996]**
  (seed 42) / 0.974 [0.940, 0.997] (seed 123) against an unstratified 0.926 / 0.923. But it
  is **not** separable from scanner model, device id or coil array: no scanner model carries
  enough subjects in both coil buckets. So the paper may say "phase encodes hardware, not
  merely site", and may **not** decompose that hardware into coil count as distinct from the
  scanner it is attached to. *`pipeline_out/robustness/s09_robustness.json`,
  `coil_vs_site.verdict`.*
- Aggregation sensitivity: 7 distinct schemes (mean, max, top-1/2/3/5 mean, q75, q90), and
  no scheme lifts any clinical cohort's cross-seed CI lower bound above 0.500. The
  selection-aware envelope — the 2.5th percentile of the best-of-7 AUC within each subject
  resample — tops out at **0.476**. Sixteen confound-cohort results *do* clear chance under
  the same schemes, so the sweep can detect an effect and the clinical null is informative
  rather than underpowered machinery. *`s09_robustness.json`,
  `aggregation_sensitivity.verdict`.*

#### 5.4ter Does the unit change *which method wins*? (~700 words, ADDED 2026-07-29)

New results subsection, drafted as **§3.3bis of `paper/DRAFT.md`**. Place it immediately after
the unit-collapse subsection (§3.3), because it is the natural follow-on question and because
placing it later invites a reviewer to think it was an afterthought. Every number from
`paper/rank_inversion.md`; nothing here may be written from memory.

Write it in this order, and do not reorder it to lead with the striking number:

1. **The question, and why it is not the same as §3.3.** §3.3 shows the unit changes the
   number. This asks whether it changes the ordering — the claim that would make a slice-level
   benchmark *selecting* rather than merely *optimistic*.
2. **The trap, before any result.** A ranking on noisy estimates differs between two units by
   chance, so slice-rank ≠ patient-rank proves nothing. State the remedy in the same paragraph:
   `D = 1 − τ_b`, between-unit disagreement compared against within-unit disagreement from
   subject resampling with the unit held fixed, paired inside each replicate, credited only if
   the paired difference clears **both** floors. Thresholds (τ = 0.50, top-1 = 0.50) pre-set in
   the file.
3. **The null, as a table.** Five cohorts; ρ, between-unit `D`, both paired δ's with intervals,
   split-half τ at both units, verdict. **0 of 447 pairs survive; 204 sign-discordant; 0 of 5
   cohorts clear both floors.**
4. **The trap firing, named.** Prostate T2 and DWI share none of their top-three methods and
   prostate T2's ρ is negative — and its patient-level split-half τ is −0.42, with all 21
   patient-level AUROCs in 0.381–0.539. Prostate T2 clears the slice floor and not the patient
   floor, which is what a ranking with no ordering information looks like. Say this in the same
   paragraph as the disjoint top-3 sets, never after it.
5. **The one supported effect, with its ceiling.** Brain unit × condition interaction +0.028
   [0.015, 0.041], p = 0.001; 6/6 positive, 4/6 Holm-supported; sign flips for
   `densenet121/imagenet` and `resnet18/imagenet`; mechanism (mean aggregation gain +0.045
   magnitude vs +0.017 phase, and +0.032/+0.023/+0.034 against +0.005/+0.004/+0.006 at matched
   slice AUROC 0.912–0.924, which rules out ceiling compression); then the cap — each individual
   ordering is a tie, so no method selection follows; then that it is a hardware label, not a
   diagnosis, and unsupported on the other four cohorts.
6. **The published literature, positives and negatives together.** CAMELYON16 two boards
   (τ = 0.754, 61/496 discordant); Ruan 2021 (τ_a = 0.603, winner changes, authors state the
   mechanism); Islam 2021 (τ_a = 0.467, authors name a different optimum per unit); then the
   two cases with intervals at both units where the inversion lies inside them (Jarkman 2022,
   Guo 2019); then the counter-example at equal prominence (Chen 2025, 11 methods, τ_a = 0.927,
   same winner); then Maier-Hein 2018 as the bar (τ 0.74–0.85 already changes winners).
7. **The rarity denominator.** 1 of 2,642 scanned full texts reports both units for ≥ 2
   comparable methods; 7 of 350 benchmark-anchored papers for ≥ 3. For most published models
   the ranking at the other unit is unrecoverable.
8. **The self-criticism, in the subsection and not only in Limitations.** Our methods are 7
   architectures × 3 conditions from one pipeline at one seed, not independent systems.

**Forbidden in this subsection:** "the unit changes which method wins", "slice-level benchmarks
select the wrong architecture", any quotation of the disjoint top-3 sets without the split-half
τ, and any presentation of the interaction as an inversion. See §2.

#### 5.4bis Prevalence: how often is the check reported? (~600 words, REWRITTEN 2026-07-29 on the final pooled result)

New results subsection. Place it **last in Results**, immediately before the Discussion, so
the reader meets the benchmark evidence first and the field-level evidence as the closing
move. Every number from `paper/FINDINGS.md` §1–§5, which is generated by
`paper/screen/analysis/pool_final.py` → `paper/screen/analysis/pooled_final.json`; nothing
here may be written from memory, and **no number from `paper/prisma_flow.json` or
`paper/screen/analysis/analysis_out.json` may be used — both are superseded** (FINDINGS §9).

Write it in this order, and do not reorder it to put the striking number first:

1. **The flow, with the censoring named immediately.** 250 records sampled from a frozen
   9,979-record PubMed frame (positions 1–100 plus reserve blocks R1–R3 at 111–260, taken in
   permutation order under the pre-registered extension rule); 115 excluded (E-SEG 39,
   E-DERIV 32, E-2D 14, E-NOCLF 13, E-NONMED 9, E-TYPE 5, E-PROJ 3); **44 eligible but
   unreachable — 32.6% [25.3%, 40.9%] of the eligible set**; **91 included and readable**,
   against a pre-registered target of 75, **which is met**. Figure:
   `paper/figures/prisma_flow_pooled.svg`.
2. **The sentence that must come next, before any prevalence number.** The extension rule was
   executed in full and *did not fix the access rate*: block-by-block the unreachable rate was
   35.6%, 34.6%, 22.7%, 32.1%. Screening 150 additional papers moved pooled censoring from
   36.4% to 32.6%. **Sample size is not the lever here; full-text access is.** Say this in
   Results, not Limitations.
3. **Agreement, before any prevalence number.** Pre-reconciliation the primary flag gave raw
   65.6%, Fleiss' κ −0.015 [−0.164, 0.120], AC1 0.479, 6/15 unanimous — below the
   pre-specified floor of 90% raw / κ 0.60, so the pre-registered remedy fired. Then the
   diagnosis: the form declared the baseline flags boolean with **no level for "could not be
   assessed"**, and every disagreement fell on records where the field is undefined; restricted
   to the six overlap papers all four screeners obtained *and* included, agreement is **100%,
   AC1 1.000, unanimously negative on all six sub-flags**. Then the post-amendment numbers:
   raw **95.6%**, Fleiss' κ **0.932 [0.777, 1.000]**, AC1 0.934, 14/15 unanimous — **both
   floors met**. Then, in the same paragraph and not in a footnote: that number is a
   *counterfactual re-encoding of the same four sealed files* under two amendments that add a
   missing level, **not an independent re-rating**, and the reserve blocks are single-coded, so
   the paper's entire reliability estimate rests on 15 papers. `split_unit` is the one field
   whose disagreement is substantive and survives the amendment (κ 0.637 [0.430, 0.824]); it is
   disclosed here because it caps S4.
4. **P1, with the bound as the headline.** Complete case 0/91 = 0.0% [0.0%, 4.1%];
   **bounding interval [0.0%, 32.6%] over 135 eligible papers, which the protocol makes the
   headline because censoring exceeds 15%.** Give the evidence-restricted denominator in the
   same breath: 0/79 = 0.0% [0.0%, 4.6%] over the papers with a fully recorded 14-term search
   including the supplement. Then the censoring-free form, which is the strongest sentence in
   the subsection: **zero TRUE flags on any of the four zero-image sub-flags across all 345
   coded records covering 300 distinct sampled papers**, including excluded and unreachable
   ones.
5. **The near misses, quoted.** These are what make the subsection readable rather than a
   table of zeros. PMID 41481488 builds a clinical-and-location-only arm that *beats* its own
   image-only network (macro AUC 0.867 vs 0.851) and reads the result as "location information
   improves identification"; PMID 33239711 tabulates label frequency against relative
   intracranial height, reports a position-stratified AUC per band, observes that the best
   band "had the most number of positive cases", and still reports no positional baseline;
   PMID 42266879 reports 95.87% accuracy on a cohort with a 77.8% no-information rate it never
   computes. Say for each why the codebook codes it FALSE. All three are in the post-hoc block
   R4 and must be labelled as such.
6. **S5, which is the one most directly on our thesis.** 1/91 = 1.1% [0.2%, 6.0%] report the
   positional distribution of their labels in a figure or table; 2/91 = 2.2% if a bare
   qualitative mention counts. State that the single positive was added by an adjudication rule
   that made the literature look *better* on this endpoint, and that the rule was adopted for
   that reason.
7. **The unit and split secondaries.** 44.0% [34.2%, 54.2%] evaluate below the patient;
   18.7% [12.0%, 27.9%] headline at the slice; **93% report exactly one unit and never contrast
   two**; of the 19 reporting any slice-level metric, 6 also report patient-level
   (31.6% [15.4%, 54.0%], n = 19, say so). 31.9% [23.2%, 42.0%] state a subject-level split —
   **with κ = 0.64 in the same sentence** — while 17/91 split at an image or slice unit and 22
   more say only "randomly split". `split_disjointness_verified` is `not_stated` for 49 of 91.
   2.2% report a subject-clustered interval; **48 of 91 report no uncertainty at all**.
8. **What exists instead.** 5/91 (5.5% [2.4%, 12.2%]) report any non-imaging comparator, and
   four of the five are the clinical-variables arm of a nomogram. Name what that comparison does
   and does not test. This is the paragraph that stops a reviewer saying "plenty of papers
   report baselines".
9. **The direction-of-bias paragraph, quoted from the pre-registered protocol §7**, now with
   the confirming measurement: paywalled articles skew to higher-tier clinical journals, the
   venues most likely to demand a comparator arm, and the exploratory venue cut bears that out —
   censoring 36.0% [26.7%, 46.6%] in clinical/radiology journals against 11.5% [4.0%, 29.0%] in
   engineering/computing ones. So silently dropping the unreachable papers would bias P1
   *downward*, toward our own thesis. That is why the bounds are unconditional. Flag the venue
   classifier as a provisional keyword heuristic, not the pre-specified scope-statement reading.
10. **The post-hoc block, in one sentence.** Block R4 (positions 261–310) was screened after the
    stopping rule had already fired at 91 included papers and is reported as a labelled post-hoc
    extension: 114 included, censoring 32.9%, P1 bound [0.0%, 32.9%], numerator still zero.

**Forbidden in this subsection:** any sentence of the form "no published paper reports…", any
point estimate without its bound, any use of "systematic review", any presentation of the
post-amendment κ without the words "the same four sealed files", and any pooling of R4 into the
pre-registered denominator. See §2.

### 5.5 Discussion (~1,200 words)

1. **What a high trivial fraction licenses, in one paragraph, stated three times in the
   paper.** It is a statement about an evaluation protocol. It is not a decomposition and
   not a claim about a model's internals.
2. **Why the collapse, not the match, is the general finding.** The match rests on one
   preprint's Table II; the collapse rests on eight of our own computations on public label
   files. Concede the first and keep the second.
3. **Where the null legitimately fires and where it legitimately does not.** DeepLesion's
   labels are anatomical regions; Duke breast's slice task is positional *by construction*
   (the owners' rule: inside the tumour box is positive, ≥5 slices away is negative,
   everything between discarded), so its 0.823 quantifies a tautology rather than indicting
   anyone (`audit_results.md` §3.4). Saying this loudly is what makes the fastMRI Prostate
   row believable.
4. **For benchmark publishers.** Three fields in the label file you already release —
   subject id, slice index or z position, official split assignment — make every rule
   auditable by people who will never be granted the pixels. PI-CAI already reports at case
   and lesion level by design. RSNA ICH is the counter-example.
5. **What the protocol does not do.** It cannot detect shortcuts that live in the pixels —
   scanner texture, burned-in annotation, body-part framing. Those need the images. It
   bounds only the part reachable *without* them, which is the part a third party can check
   for free.
6. **Relation to prior work, again, at the end.** Badgeley's control is a negative control
   (balance the confounder, watch the model collapse to 0.52); ours is a positive control
   (fit the confounder alone, see what it reaches). Same phenomenon, opposite direction, and
   the positive-control form is the one an auditor can run without the images. That is a
   difference in *form*, not in *discovery* (`audit_targets.md` §3.2.2).

### 5.6 Limitations — see §7. This is a full section, not a paragraph.

### 5.7 Data and code availability

- Tool: `trivialbaselines/`, MIT, `numpy` + `pandas`. Resolve the PyPI-vs-git install
  discrepancy first.
- Every audit payload: `pipeline_out/trivial_baselines/*.json` + `*.md`, one card per run.
- Label-table preparation: `pipeline/audit_prep/` (8 scripts).
- Label file provenance with SHA-256: Table 1.
- The worked example's full report: `pipeline_out/report/RESULTS.md` and `verdict.json`.
- **`manuscript/*.docx` must not be released.** `manuscript/DO_NOT_SUBMIT.md` records that
  those files report numbers no code in the repository has ever produced. Delete them from
  any public archive or keep them only in a clearly quarantined directory.

---

## 6. Figures and tables

### Figures

**Figure 1 — What the audit needs.** Schematic. Left: the four columns (subject id, slice
index, label, split) as they appear in a real published CSV. Middle: the five baselines.
Right: the card the tool emits. One annotation: "no pixels, no DUA, no GPU; numpy + pandas".
*Source: `trivialbaselines/README.md`; schema in `audit_targets.md` §1.1.*
New artwork required.

**Figure 2 — The unit-of-evaluation collapse.** Slope chart: eight dataset-arms, slice-level
AUROC on the left axis joined to patient-level AUROC on the right, subject-clustered CIs on
both. Colour: three collapsing (fastMRI Prostate ×2, fastMRI+ knee ×2 — five lines), one
undefined at the patient level (Duke breast, drawn as a line running off the panel with the
reason printed), two not collapsing (DeepLesion pelvis, LUNA16). **The exceptions must be in
the same panel, same colour scale, not in a supplement.**
*Source: `audit_results.md` §4; the eight cards in `pipeline_out/trivial_baselines/`.*
New artwork required.

**Figure 3 — The fastMRI Prostate waterfall.** Eight rungs on one axis:
W0h their headline 0.861 [0.843, 0.879]; W0 their PCA arm (0.813 magnitude / 0.809
magnitude+phase); W1 zero-image positional on their labels 0.851 [0.821, 0.880]; W1p the
same scores at patient level 0.424 [0.298, 0.547]; W2 our reimplementation at their
evaluation level 0.574 / 0.616; W3 the same predictions with a subject-clustered interval
0.574 [0.489, 0.667] / 0.616 [0.528, 0.691]; W4 patient level 0.524 [0.348, 0.690] / 0.528
[0.356, 0.696]; W4s position held fixed 0.467 / 0.562.
Annotation, mandatory: *"W2 does not reproduce W0. This figure is about the evaluation
protocol, not about their model."*
*Source: `pipeline_out/s12_arm_mag.log:127-134`;
`pipeline_out/s12_waterfall_magphase.log:427-435`.*
New artwork required; data already persisted.

**Figure 4 — Trivial fraction across the 12 scored rows.** Forest plot, one row per
(dataset, published number), ordered by fraction, with a vertical line at 1.0 and verdict
labels. Rows 1–6 fastMRI Prostate, 7–9 DeepLesion, 10 LUNA16, 11–12 PI-CAI. LUNA16's row is
its CPM comparison, drawn on its own scale with the scale change made obvious.
*Source: `audit_results.md` §2.1.* New artwork required.

**Figure 5 — The remedy.** Paired bars: raw slice AUROC vs position-stratified AUROC for
four score vectors (zero-image T2 0.854→0.546; zero-image DWI 0.851→0.539; our magnitude arm
0.574→0.467; our magnitude+phase arm 0.616→0.562), plus a small inset showing the bin sweep
(5/10/20/50) and the no-fit centrality value so binning cannot be blamed.
*Source: `pipeline_out/rempe/positional_baseline*.json`; `s12_*` logs, rung W4s.*
New artwork required.

**Figure 6 (worked example, main text or supplement) — the confound cohorts.** Phase vs
magnitude at predicting an acquisition property, with the interpretation inverted and the
inversion printed on the panel: brain, receive-coil count, phase 0.921 [0.870, 0.966] vs
magnitude 0.913 [0.872, 0.950], 136 test subjects; knee, pulse sequence, 0.999 vs 1.000, 29
paired subjects. **Never on the same axis as a diagnostic AUROC.**
*Existing artwork: `pipeline_out/report/figures/fig6_confound_predictability.png`.*

**Supplementary Figure S1 — Bootstrap coverage.** Clustered 91.5% (width 0.370) vs naive
46.5% (width 0.117), 3.18×, on simulated data with true AUC 0.6880.
*Source: `pipeline/s04_stats.py --self-test` block [6].* New artwork required.

**Supplementary Figure S2 — Reconstruction fidelity, worked example.**
*Existing artwork: `pipeline_out/recon_fidelity/fig_recon_fidelity.png` / `.pdf`.*

**Supplementary Figure S3 — Positional label-rate histograms.** Label rate against relative
slice position, training set only, one panel per audited arm. This is Rule 5's own
deliverable and the paper should ship it for its own data.
*Source: the `positional` blocks of `pipeline_out/trivial_baselines/*.json`.*

### Tables

**Table 1 — The eight label files.** File, bytes, SHA-256 prefix, source URL, licence, and
a column "sufficient on its own?" with the honest values (fastMRI Prostate yes ×2,
DeepLesion yes, Duke breast yes-with-TCIA-series-metadata, PI-CAI yes, LUNA16 yes,
fastMRI+ **no — needs HDF5 headers**).
*Source: `audit_results.md` §6.*

**Table 2 — The trivial fraction across every benchmark and every comparator.** A
distribution panel first (five row-sets: peer-reviewed strongest-per-arm, peer-reviewed all,
strongest-per-arm any comparator, all rows, preprint only) then the 24 rows: dataset,
published number, source, peer-review status, our best zero-image baseline, **trivial
fraction with CI**, verdict as a secondary column; the three no-comparator rows in a separate
block with the reason.
*Source: `paper/trivial_fraction_distribution.{json,md}`; `audit_results.md` §2.0–2.2.*

**Table 3 — Slice vs patient, same score vector, same estimator.** Panel A: RSNA ICH, all six
labels, all 18,938 patients, with the max-aggregated reading and the permutation null.
Panel B: the other seven dataset-arms plus the position-stratified remedy.
*Source: `pipeline_out/trivial_baselines/rsna_ich_unit_collapse.json`; `audit_results.md` §4.*

**Table 3bis — Does the unit change the ranking?** One row per cohort: methods, subjects,
Spearman ρ with interval, between-unit `D = 1 − τ_b`, the two paired δ's against the
within-unit noise floors with intervals, split-half τ at both units, top-1 reproducibility at
both units, verdict, and pairs examined / sign-discordant / surviving. A second panel for the
brain unit × condition interaction: per-architecture `d_slice`, `d_agg`, `I` with interval,
Holm p, sign-flip flag. The caption must contain the sentence "no cohort's between-unit
disagreement exceeds its within-unit resampling noise, and no inversion pair survives".
*Source: `pipeline_out/rankinversion.json`; `paper/rank_inversion.md` §3–§4.*

**Table 3ter — Published two-unit comparisons.** One row per located case: citation, unit
pair, n methods, τ, whether the top-1 changes, whether intervals are published at both units,
whether unit and metric are confounded, and the caveat that must travel with it. Positives and
the two negative controls in the same table, not in separate ones.
*Source: `paper/published_inversions.json`, `paper/published_inversions_round2.json`;
`paper/rank_inversion.md` §5.*

**Table 4 — The protocol, seven rules, each with the measured failure behind it and the
checklist items it maps to.**
*Source: `paper/protocol.md`, `paper/checklist.md`.*

**Supplementary Table S1 — Per-column metadata results** for every audited arm (best single
columns with the multiplicity warning attached), plus the exclusion lists.
*Source: the "Single metadata columns" block of every card; `audit_results.md` §3.5.*

**Supplementary Table S2 — The worked example's nine criteria** for all three clinical
cohorts, verbatim from `pipeline_out/report/RESULTS.md` §2, including the two PASSes
(breast C3/C8, prostate T2 C7) so it cannot be read as a table built to fail.

**Supplementary Table S3 — DeepLesion per-class zero-image ceilings**, official split, with
the "reference level, not a debunking" caveat in the caption.
*Source: `deeplesion_*_vs_rest.md`.*

---

## 7. Limitations — the full section, written to be pasted

Write it in the first person plural, in the main text, before the conclusion, and do not
soften any of it.

**We could not reproduce the pipeline of the paper we audit most closely.** Our
implementation of Rempe et al.'s protocol on our own prostate DWI cache reaches 0.616
slice-level AUROC against their reported 0.809 for the magnitude+phase arm, and 0.574
against their reported 0.813 for the magnitude arm
(`pipeline_out/s12_waterfall_magphase.log:431`, `pipeline_out/s12_arm_mag.log:131`). We
therefore have no standing to make any claim about their model. Everything we say about
their benchmark is a claim about the *evaluation protocol*: that a model with no access to
the pixels reaches 0.854 [0.812, 0.891] on their own published label file and split, against
their reported 0.861. Whether their network learned tumour signal is a question a label file
cannot answer and we do not attempt it.

**The rank-inversion analysis compares configurations we trained, not published systems.** The
21 method cells share one training pipeline, one preprocessing path, one optimiser schedule and
one seed, and they are 7 architectures × 3 input conditions rather than 21 independent methods.
A configuration set assembled by one group has far less between-method spread than a challenge
leaderboard — CAMELYON16's 32 independent submissions span slide AUC 0.78–0.99 — which is the
most likely reason our between-unit signal sits inside the noise while theirs does not. This
arm is an internal consistency check on our own models and must not be read as evidence about
the field. `paper/rank_inversion.md` §7.

**The rank-inversion null is a statement about these cohorts and this design, not an absence.**
The bootstrap resamples subjects, not training runs, so seed-to-seed variability appears in
neither the between-unit nor the within-unit term; that omission makes the within-unit floor too
*low* and biases the test **towards** declaring a unit effect, and we found none regardless. The
floors themselves (`D` ≈ 0.21–0.71 in rank distance) are the minimum detectable effect and should
be quoted as such. Two cohorts carry three methods each, at which width Kendall τ spans [−1, 1];
the knee cohort is reported as no estimate because all three methods reach 1.000 at the
aggregated unit.

**The published-inversion search ran without a working web search.** Both passes had WebSearch
returning API errors; coverage came from the Europe PMC full-text API (3,462 records harvested,
2,934 full texts scanned), the arXiv API, the GitHub API and direct leaderboard fetches. Kaggle
is JavaScript-rendered and its solution discussions were not searched. The rarity denominators
are lower bounds on coverage. Every FROC-versus-AUC case also confounds unit with metric, since
AUC is not definable at the lesion level.

**The single MATCHED benchmark's published comparator is a preprint.** arXiv:2407.06165,
v2 dated 14 Apr 2025, carries no journal reference as of 2026-07-29. If it remains
unpublished at submission, the strongest row in this paper is a comparison against
non-peer-reviewed numbers and should be described that way in the results, not only in the
limitations.

**The phenomenon is known; the measurement is what is new, and even that has closer prior
art than we first recorded.** Shortcut learning (Geirhos 2020), acquisition and process
confounding (Badgeley 2019; DeGrave 2021; Ong Ly 2024), slice-level split inflation (Yagis
2021; Tampu 2022; Wen 2020) and leakage as a reproducibility failure (Kapoor & Narayanan
2023) are all established. During this audit we found a published position-only baseline on
one of our own targets: Yan et al., CVPR 2018, Table 1, "Baseline: Location feature", 59.7%
8-class accuracy on DeepLesion against their own 90.5%. Their location feature is
image-derived — z comes from a self-supervised body-part regressor — and is used as a
retrieval feature rather than as a critique, and our pixel-free version lands at 0.557; but
the idea of a location-only baseline on this benchmark is theirs and not ours. What remains
ours is that the position can be taken from the *published label file* with no image and no
regressor, the uniform application across benchmarks with identical reporting, and the
released tool. **Before submission the prior-art search must be redone properly against
Google Scholar, the MICCAI/MIDL/ML4H proceedings and the RSNA ICH Kaggle write-ups, not
against the handful of web queries recorded in `audit_targets.md` §3.4.**

**Only one benchmark was matched, all six MATCHED rows are that benchmark, and its comparator
is a preprint.** Across 24 (benchmark, published comparator) rows the median trivial fraction
against peer-reviewed comparators is 0.469, and **zero** peer-reviewed rows are matched. The
general statement "trivial baselines match published performance" is not supported by these
data, and we do not make it. What we do make is the quantitative statement, and it must not be
inflated into the categorical one at any point in the writing.

**Two audited benchmarks are not pixel-free in the same clean sense.** fastMRI+ needs slice
counts from the fastMRI HDF5 headers because it publishes positive annotations only, and our
coverage is 199 of 1,173 roster volumes (17%), which is not the subset any published number
would use. LUNA16's FP-reduction track is conditioned on `candidates_V2.csv`, a candidate
list produced by image-based detectors, so "zero-image" there means "zero image given the
published candidate list".

**Two audited rows are positional by construction, not by accident.** Duke breast's slice
task is defined by distance from the tumour box, so a high positional null there is a
tautology we quantify rather than a confound we discover; and every patient in that cohort
has cancer, so the patient-level AUROC is undefined (922 of 922 positive) and we report it
as unavailable. DeepLesion's eight classes are anatomical regions, so position predicting
them is the task.

**The PI-CAI comparison is across cohorts.** The published 0.91/0.86 are on the hidden
1,000-case testing cohort (400-case subset for the reader comparison); our baseline is on the
public 1,500-case Training and Development set. A strict reading makes those rows
non-comparable. We score them because the caveat runs against the null.

**The trivial fraction's interval is too narrow as a statement about the ratio.** It
propagates uncertainty in the baseline only; the published number enters as a fixed
constant, because we almost never have its sampling distribution. Where the publication
gives a half-width (Rempe et al. report ±1.8 on 0.861) we report it but do not combine it,
because the resampling unit behind their interval is not stated to be the subject.

**Our own clinical cohorts are small, single-institution, single-vendor and 3 T only.**
Prostate T2 n=67, prostate DWI n=45, breast n=70. Official test folds are 4–7 subjects,
which is why the cross-validated pooled out-of-fold estimate is the headline and the
official split is reported only as a labelled secondary analysis. A null on 67 patients is
a null on 67 patients, not a statement about MRI phase.

**Our own pipeline fails one of its own controls on its primary cohort.** The label
permutation null for prostate T2 has an observed range of [0.548, 0.645] over 20 distinct
replicates, which does not contain 0.500. That is a failure of criterion C3, and it means
nothing downstream of it on that cohort can be believed on its own terms. We report the
cohort as NOT SUPPORTED partly for that reason, and we state it here rather than in a
supplement.

**The remedy is validated on four score vectors, not on a benchmark suite.**
Position-stratified AUROC collapses the zero-image nulls (0.854→0.546, 0.851→0.539) and
moves our two trained arms (0.574→0.467, 0.616→0.562). We have not demonstrated that it
preserves a genuine effect, because we do not have a benchmark with a demonstrated genuine
slice-level effect to test it on. That is the most important missing validation in this
paper and it should be named as future work in those words.

**Reconstruction fidelity is validated for magnitude only.** Every vendor reference in these
releases is a magnitude image. The phase channel is never directly validated and inherits
credibility only through sharing the same complex image. The breast reference (`temptv`) is
the vendor's temporal-TV-regularised reconstruction of the same radial k-space, not an
independent ground truth, so r = 0.977 there is agreement between two estimators and is the
weakest of the five comparisons.

**The audit is a snapshot of what one analyst could obtain without accepting a data-use
agreement.** RSNA 2019 ICH, RSNA 2023 Abdominal Trauma and RSNA 2022 Cervical Spine are all
behind click-through agreements that were not accepted; PI-CAI's slice-level arm would need
1,295 lesion-delineation volumes and a NIfTI reader. Those are the obvious next targets and
their absence is a limit on the audit's breadth, not evidence about those benchmarks.

---

## 8. What a reviewer will attack, and how the paper pre-empts it

### 8.1 "This is known. Yagis 2021 and Badgeley 2019 already showed it."

*Pre-empt:* the introduction's second paragraph, before any of our results, and again in the
discussion. Concede fully, then make the distinction that survives: prior work measures the
inflation caused by a *wrong* split; we measure what remains under a **correct,
patient-disjoint** split. Rempe et al.'s split is patient-disjoint. State this in one
sentence in the abstract.
*Residual risk:* high. This is the most likely rejection reason and the concession must be
generous enough that the reviewer feels read rather than argued with.

### 8.2 "Yan et al. 2018 already published a location-only baseline on DeepLesion."

*Pre-empt:* we found it ourselves and it is in the limitations, in the DeepLesion results
paragraph, and cited in the introduction. Our 0.557 sits *below* their 0.597, and we say so.
The remaining difference is pixel-free versus image-derived position, and it is stated as a
difference in form.
*Residual risk:* medium, and rising if the proper prior-art search finds more. Do that search
before submission (§`COLLABORATORS.md` is not the right place for it — it is our job).

### 8.3 "Prostate cancer really does concentrate in the mid-gland. Your positional 'null' is anatomy."

*Pre-empt:* three moves. (i) Agree that it is anatomy — that is the point: an evaluation
protocol that credits anatomy to a model is the problem, not the anatomy. (ii) Show that the
patient-level reading of the *same scores* is 0.506 / 0.424, so whatever the model ranks
correctly at the slice level it does not rank patients. (iii) Show the remedy: stratifying
on position collapses the null to 0.546 / 0.539, which is the metric we ask reviewers to
require. Add a clinician co-author's one-paragraph statement on the anatomical expectation
(`COLLABORATORS.md` §3) so the concession is authoritative rather than defensive.
*Residual risk:* medium-low once the clinical paragraph is in.

### 8.4 "You compare against a preprint."

**This was the disqualifying attack and it has been defused structurally, not rhetorically.**
The flagship no longer involves a published number of any kind: it is the RSNA ICH
slice-to-patient divergence on 18,938 patients (§1), computed from a public label file, so
there is nothing to compare against and nothing to dispute. The audit's headline is the
*distribution* of the trivial fraction, whose primary line is restricted to peer-reviewed
comparators (median 0.469, n = 9). Rempe et al. now appears as a worked example, in one
subsection, labelled a preprint in its own heading and in its first sentence, and **deleting
it entirely changes no headline claim in the paper.** Test that literally before submission:
strike §3.2 and Table 2 rows 19–24 and confirm the abstract still stands.

*Remaining pre-empt:* say it in the results the first time the number appears, not only in
the limitations. Check the publication status again the week of submission. Offer Rempe et
al. sight of the critique in advance and record their response (draft email in
`COLLABORATORS.md` §4).
*Residual risk:* **low**, downgraded from medium on 2026-07-29. It is now an attack on a
worked example rather than on the paper.

### 8.5 "Your tool only ever confirms itself."

*Pre-empt:* four built-in negatives, all in the main text, and now expressed as numbers on
the same continuous scale as the positives rather than as a verdict label — which is what
makes the defence legible. **LUNA16's trivial fraction is −0.002**, below its own
random-score reference, on the challenge's own metric (CPM 0.0020 against 0.0027).
**PI-CAI's positional baseline is exactly 0.500** at every bin setting, the correct
registration of "inapplicable". The synthetic clean control shipped with the tool sits at
−0.041 [−0.338, 0.304]. And the two confound cohorts from the worked example give brain
positional 0.480 [0.446, 0.513] and knee positional 0.500. Of the nine peer-reviewed
head-to-heads, eight lie in 0.395–0.613 and one is at the floor — that spread *is* the
specificity argument. State the limit in the same breath: two negatives out of nine rows is
an existence proof that the measure can fail to fire, not an estimate of its false-positive
rate, and no such estimate is claimed.
*Residual risk:* low. This is the paper's strongest structural defence and it should be in
the abstract.

### 8.6 "Slice-level AUROC is a legitimate metric for localisation, not diagnosis."

*Pre-empt:* agree, and carve the scope explicitly in the methods. The critique is of
slice-level numbers presented as evidence about *patients*. Duke breast is the worked
example of a genuinely within-patient localisation task, and we say its 0.823 is a
tautology. Add one sentence: "where the clinical question really is localisation, report it
as localisation and report the patient-level number too, because readers will otherwise
assume it."
*Residual risk:* low.

### 8.7 "The trivial fraction's confidence interval is wrong."

*Pre-empt:* stated as a limitation in `protocol.md` Rule 7, in every generated card, and in
the methods. It propagates baseline uncertainty only. Ask the biostatistician
(`COLLABORATORS.md` §2) whether a defensible two-source interval is available when the
publication reports only a ± half-width without naming the resampling unit; if not, say so
in the paper and keep the one-sided reading (the interval is a statement about our baseline,
not about the ratio).
*Residual risk:* medium. A statistically strong referee will press here, and the honest
answer — "we cannot, and here is why" — is acceptable only if it is offered first.

### 8.8 "MATCHED is defined by a CI upper bound touching a point estimate. That is not a test."

*Pre-empt:* the rule is stated before any result (`audit_results.md` §1) and it is
deliberately generous to the *benchmark* in one direction and to us in another. Say plainly
that it is a descriptive decision rule, not a hypothesis test, and that no p-value is claimed
from it. Ask the biostatistician whether an equivalence framing (TOST against a margin) would
be stronger and whether it would change any verdict.
*Residual risk:* medium. This is the second most likely statistical objection.

### 8.9 "n = 46 test patients. The intervals are enormous."

*Pre-empt:* they are, and they are reported clustered on patient. 1,399 slices from 46
subjects with 68 positive slices and 20 positive patients (T2). Report positive-patient
counts everywhere, as the checklist demands of everyone else. Note that this is *their*
official test split, not one we chose, and that the small-n objection applies with equal
force to the published 0.861.
*Residual risk:* low, and it cuts both ways.

### 8.10 "You audit a benchmark you also use as your own study's dataset. That is circular."

*Pre-empt:* separate the two roles explicitly in the methods. The audit uses Rempe et al.'s
published 312-patient label CSV and their own `data_split` column; our study uses a
locally reconstructed 45/67-patient cache with its own subject-level CV. Different tables,
different splits, different subjects. The waterfall figure shows both and labels which rung
comes from which.
*Residual risk:* low if the figure is labelled; high if it is not.

### 8.11 "Your own study is a null result on 45–70 patients. Why is it in the paper?"

*Pre-empt:* say what it is for in the first sentence of that section: it is the worked
example of applying the protocol to one's own work, including reporting a control our own
pipeline failed. It is not evidence about MRI phase. Cap it at ~400 words in the main text.
If a reviewer still objects, it moves to the supplement in full and the main text keeps only
the metadata-baseline row (breast `folder` 0.743 vs network 0.633) and the coil-count
result (0.921), both of which are protocol evidence rather than phase evidence.
*Residual risk:* medium. Be ready to move it.

### 8.12 "The 20-bin choice is arbitrary."

*Pre-empt:* the bin sweep is reported for every arm (T2: 5=0.835, 10=0.848, 20=0.854,
30=0.854, 50=0.856) and the *no-fit* centrality score — which uses no training data at all —
reaches 0.825 (T2) and 0.841 (DWI). The result does not depend on fitting anything.
*Residual risk:* very low.

### 8.13 "`pip install trivialbaselines` does not work."

*Pre-empt:* fix it before submission. Either publish to PyPI or change the command in
`protocol.md` and `checklist.md` to match `trivialbaselines/README.md`. A reviewer who tries
the advertised command and gets a 404 will distrust everything else.
*Residual risk:* certain if unfixed, zero if fixed.

### 8.14 "Seven benchmarks is not systematic."

*Pre-empt:* do not claim it is. Claim eight label files, 24 comparator rows, uniform reporting, a
stated entry criterion and a published exclusion list — and name the targets not reached and
why (`audit_results.md` §6). An audit that reports what it could not reach is more credible
than one that reports only what it could.
*Residual risk:* medium, and it is the reason for the venue recommendation in §3.

### 8.15 "Your rank analysis compares seven architectures you trained yourself. That is not a benchmark of methods."

This is the strongest objection to §3.3bis and it is correct. All 21 cells share a pipeline, a
preprocessing path, an optimiser schedule and a seed; they are 7 architectures × 3 input
conditions, not 21 independent systems. A one-group configuration set has far less
between-method spread than a leaderboard of 32 independent submissions, which is very likely
why our between-unit signal sits inside the noise.

*Pre-empt:* say it in the results subsection, not only in Limitations, and say what would close
it — per-algorithm scores at both units from a benchmark that publishes both. Name PI-CAI
explicitly: 293 submissions, hidden 1,000-scan cohort, official ranking already
`(lesion-level AP + patient-level AUROC)/2`. State that we sought that table and could not
obtain it (JavaScript leaderboard, authenticated API). An analysis that names the data it
needs and could not get is stronger than one that pretends its design was sufficient.
*Residual risk:* **high**, and it caps what §3.3bis may claim. It does not damage the rest of
the paper, because the null is reported as a null.

### 8.16 "You report a significant interaction between two non-significant differences."

*Pre-empt:* the interaction is a **paired difference-of-differences** computed on shared
bootstrap draws (`condition_interaction` in `pipeline/s16_rankinversion.py`), which is the
standard remedy for that error rather than an instance of it — the reviewer is describing the
error of comparing two separate significance tests, which is exactly what this avoids. Then
concede the substance immediately: neither individual ordering is established, so no method
selection follows, and the paper says so in the same paragraph as the interaction.
*Residual risk:* low, provided the concession is never separated from the result.

### 8.17 "Your null just means you were underpowered."

*Pre-empt:* agree, and quantify. Report the within-unit noise floors as the minimum detectable
effect (`D` ≈ 0.21–0.71 in rank distance), and report the split-half τ values, which are the
ones that make the null informative rather than empty: two disjoint halves of the same subjects
rank the same methods at τ = −0.42, −0.24 and −1.00 at the patient unit. That is not a
power failure to detect a between-unit difference; it is direct evidence that neither ordering
exists to be detected. Also state that the bootstrap omits seed variance, which biases the test
*towards* a unit effect.
*Residual risk:* low.

---

## 9. Blockers to clear before submission

Ordered by how much damage each does if missed.

1. **Redo the prior-art search properly.** Google Scholar, MICCAI/MIDL/ML4H proceedings,
   RSNA ICH Kaggle solution write-ups. Yan et al. 2018 was found only during the audit;
   assume there are others. (§8.2)
2. **Check whether arXiv:2407.06165 has been published**, the week of submission. (§8.4)
3. **Fix the install command** so `protocol.md`, `checklist.md` and
   `trivialbaselines/README.md` agree. (§8.13)
4. **Reverse the T2/DWI recommendation in `paper/audit_targets.json`** — T2 is the correct
   arm; the persisted artefact is already right; the docstring at
   `pipeline/s12_rempe.py:272-278` is wrong. (§5.4 R2)
5. **Send the three collaborator requests** in `paper/COLLABORATORS.md`, with the Rempe
   email first because it has the longest turnaround.
5b. **Request the PI-CAI per-algorithm AP and AUROC table** from the challenge organisers, or
   read the leaderboard in a browser that executes JavaScript. 293 submissions, hidden
   1,000-scan cohort, official score already `(lesion-level AP + patient-level AUROC)/2`. This
   is the only route that could convert the §3.3bis null into a positive result, and it is the
   highest tier-per-hour item left in the project (§3.4, §8.15). Not a blocker for submission
   to Radiology: AI; a blocker for any attempt at npj Digital Medicine on the unit question.
6. **Delete or quarantine `manuscript/*.docx`** from anything public.
   (`manuscript/DO_NOT_SUBMIT.md`)
7. **Replace every occurrence of "within 0.018"** with the three per-cohort background-only
   gaps (0.025 / −0.009 / 0.037). The 0.018 is a mean across cohorts and no cohort attains
   it. (§5.4 R9)
8. **Decide, in writing, whether the worked example is main text or supplement**, and hold
   to it. (§8.11)
9. **Reconcile three small numeric inconsistencies between our own documents**, because a
   reviewer who finds one in a paper about other people's numbers will look for more:
   - the fastMRI+ knee roster is **1,173 volumes** in `audit_results.md` §3.3 and **1,172**
     in `audit_targets.md` §1.2. Recount from `knee_file_list.csv` and fix both;
   - LUNA16's positional slice interval is **[0.513, 0.558]** in the persisted card and the
     JSON (`slice_ci_clustered` = 0.5135–0.5580) and **[0.514, 0.558]** in
     `audit_results.md` §4. The card is authoritative; `DRAFT.md` already uses it;
   - the DeepLesion majority-class anchor is **0.2361** in the Yan-conditions run and
     **0.2233** in the earlier official-split probe (`audit_targets.md` §2.4). They are
     different partitions and both are correct, but the draft must never put them in the
     same sentence without saying which is which.
